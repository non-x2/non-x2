# 🌱 自己改良バックログ（アイデア帳＋改良の記録）

> のんラボを毎日すこしずつ良くしていくための「共有の脳」です。
> - **クラウドの毎朝のRoutine「🌱 毎日の自己改良」**（毎朝6時）が、ここを読んで小さな改良を1つ実行し、ドラフトPRを作ります。
>   - 前の🌱PRがまだマージされていない日も**止まりません**。**いちばん新しい🌱PRに積み増し**ていき、そのPRに改良が5件たまったときだけ「そろそろレビューをお願いします」とお伝えします。
> - **ローカル（デスクトップアプリ）でも「/kaizen」と頼めば同じ型で動きます**。
> - やり方の決まりは `.claude/skills/kaizen/SKILL.md` にあります。**マージは必ずのんさんが行います**（自動では反映されません）。
> - 止めたいときは「毎日の自己改良を止めて」とClaudeに頼めばOKです。

## 使い方

- **候補**：これからやりたい改良のリスト。誰でも（どのセッションでも）気づいたら追記してよい。
  - 済んだ（✅）・見送った（❌）候補は、**番号はそのままで「📦 済んだ候補・却下した候補」へ移す**。こうすると上の表には「これからやること」だけが並ぶ（消さないのは、同じ提案がまた出たときに見せるため）。
  - 移した行は「**どんな提案だったか**」だけを残し、やったことの詳しい説明は書かない（「→ ✅ 日付に実施（記録を見る）」まで）。**同じ話を2か所に書かない**ため——くわしくは下の「📗 改良の記録」の同じ日付の行に一本化する。⚠️却下（❌）の行だけは**理由そのものが大事**なので短くしない。
- **要相談**：勝手にやってはいけないもの。のんさんの判断が出るまで動かさない。
  - 候補のうち「**のんさんの判断が出るまで、どうやっても着手できない**」ものは、番号をそのままで**ここへ移す**。上の「💡 候補」に置いたままだと、毎朝のRoutineが**毎日えらび直しては毎日見送る**ことになり、いつまでも順番が回らないため（移すときは「はい／いいえ」で答えられる形に書く）。
- **改良の記録**：実際にやったことの履歴。毎回1行追記する。

---

## 💡 候補（小さい順・上からやる）

> ここに残っているのは**まだ手を付けていない候補だけ**です。
> 済んだもの・却下したものは下の「📦 済んだ候補・却下した候補」に移してあります（番号はそのまま）。

| # | アイデア | 出どころ |
|---|----------|----------|
| 6 | 📷 台帳：月山道路の1台は北5.5km（鶴岡市）と東5.5km（西川町）のどちらも妥当で、探す順番で機械的に決まっている。**「推定値」であることをページ側でも分かるようにするか**は要検討 | 2026-08-19 |
| 8 | 👀 `tools/check_a11y.js` は「その1点を押したら何に当たるか」で測るので、**クリックを別の層で振り分ける方式（交通ページの丸＝ボロノイ）の本当の押しやすさは測れない**。PR #79 でやったような「本物のクリックを網の目に飛ばして数える」測り方を、道具として残せないか（#83 では注意書きで代用） | 2026-08-20 自己改良中の気づき |
| 21 | 👀 `tools/check_docstrings.py` はいまコマンド例と期待値を `TARGETS` に**手で書き写す**方式（docstringの文字列を自動で読み取ってはいない）。他の道具（`typhoon_snapshot_draft.py` など）にも広げるなら、docstringの中の「出力例」ブロックを自動で読み取ってパースする方式に育てられないか（要検討・パースが複雑になりすぎない範囲で） | 2026-09-02 候補20を実施中の気づき |
| 27 | 📦 `npm run check` はいま通信のいらない3つ（📚出力例・🗾予報区・🗺構成表）だけ（🔄 2026-09-16 に修正：候補29で `check:map` が加わって**2つ→3つ**になっていた）。リンク点検（85本・数分かかる）と④の見張り番（気象庁へ通信）は、時間がかかる／外に出るので**わざと外してある**。「時間はかかってもいいから全部」という `npm run check:all` を足すかは要検討（速い点検と遅い点検を混ぜると、ふだん使いの `check` が重くなるため） | 2026-09-09 候補26を実施中の気づき |
| 30 | 🗺 `tools/check_map.py` はいま `tools/` の中だけを見ている。同じ「地図と実物のズレ」は他のフォルダでも起こりうる（例：`.claude/skills/` のスキルが増えたとき・`docs/` の手順書が増えたとき）。対象フォルダを1行足すだけで増やせる形に育てられないか（まずは `.claude/skills/` から。フォルダの構成表は入れ子になっていて読み取りが少し難しいので、複雑になりすぎない範囲で） | 2026-09-11 候補29を実施中の気づき |
| 32 | 👀 📦の行から「📗 改良の記録」の日付へ案内する形にしたので、**記録の行に候補番号が書いてあること**が道しるべになった。ところが1件（候補7・2026-08-20）は番号が書かれておらず、同じ日付の2行のどちらか分からなかった（今回その場で補った）。同じ日に2件やった日は今後もあるので、`tools/check_map.py` のような小さな点検で「📦の各行が指す日付の記録に、その候補番号が書いてあるか」を照合できないか（照合する材料はどちらもこのファイルの中にあるので、通信は不要）。🔄 **2026-09-15 に手で照合してみたところ、いまはズレ0件**（📦の22行すべて、案内先の日付の記録に候補番号が書いてあった）＝作るとしたら「これから崩れないように見張る」回帰防止の道具になる | 2026-09-13 アイデア帳の整理中の気づき |
| 33 | 🧹 🙋要相談の「④が古い」行には**放っておくと毎日ズレていく数字**（◯日前）が直に書いてあったため、毎朝の整理でそこだけ書き替える作業が続いていた（2026-09-14・09-16 の2回）。2026-09-17 に「最後に測った日つきの数字＋いつでも `npm run check:typhoon-snapshot` で最新を確かめられる」形に直した。同じ種類の**時間とともに嘘になる数字**がアイデア帳の他の場所にも無いか、ときどき見直す（いまのところ他には見つかっていない。増えるようなら小さな点検の道具にするかは要検討） | 2026-09-17 アイデア帳の整理中の気づき |

## 📦 済んだ候補・却下した候補（番号順・記録用）

> 実施済み（✅）と、調べたうえで見送った（❌）候補の置き場です。**消さずに残します**——
> 同じ提案がまた出てきたときに「それは前に試して、こうなりました」と見せられるようにするためです。
> （例：候補2は実測して悪化すると分かったので却下しています）

| # | アイデア | 出どころ |
|---|----------|----------|
| ~~1~~ | ~~🚗 交通量の丸をボロノイ方式に~~ → **✅ 2026-08-19 に実施**（くわしくは下の「📗 改良の記録」の 2026-08-19 の行） | 2026-08-19 実施済み |
| ~~2~~ | ~~🚗 丸の間引きを円の距離に変える~~ → **❌ 実測して却下（2026-08-19）**。助手の指摘は**逆**でした。いまのAND判定は斜めの2点を「見逃す」のではなく**むしろ強めに間引いて**います（横6px・縦14pxは、ANDなら間引かれ、円の距離だと15.2px＞15pxで**残ってしまう**）。実際に変えて測ると 丸 67→84個・押し間違い 11→89点（0.2%→1.7%）と**悪化**。**現行のままが正解**です。同じ提案が出たらこの行を見せてください | 2026-08-19 実測で却下 |
| ~~5~~ | ~~🛟 防災ページ：⚡雷と☔大雨は同じ警報データを共有しているので「情報の古さ」がいつも同じ値になる~~ → **✅ 2026-08-20 に実施**（くわしくは下の「📗 改良の記録」の 2026-08-20 の行） | 2026-08-19 助手の指摘 → 実施済み |
| ~~7~~ | ~~👀 `tools/check_a11y.js` の押しやすさ測定は**上下方向だけ**（左右は見ていない）~~ → **✅ 2026-08-20 に実施**（くわしくは下の「📗 改良の記録」の 2026-08-20 の行） | 2026-08-19 PR #80 から引き継ぎ → 実施済み |
| ~~9~~ | ~~🌀 ④予備の文章の更新は**手作業で15か所**あり、書き落としが起きやすい。`data/latest.json` から④の下書きを**自動生成**できないか（※**下書きまで**にとどめるのが肝）~~ → **✅ 2026-08-26 に実施**（くわしくは下の「📗 改良の記録」の 2026-08-26 の行） | 2026-08-22 → 実施済み |
| ~~10~~ | ~~🗾 ④の手描き地図は台風の位置を**手で計算して置いている**（メルカトル図法の式は `index.html` の中にある）。座標を出す小さな道具を `tools/` に残せば、次回の更新が速くなる~~ → **✅ 2026-08-22 に実施**（くわしくは下の「📗 改良の記録」の 2026-08-22 の行） | 2026-08-22 実施済み |
| ~~11~~ | ~~🗾 逆方向（x,y→緯度経度）の変換も `tools/typhoon_map_coords.py` に足せないか。④の地図を見ながら「ここが今どのへんか」を確かめたいときに使える~~ → **✅ 2026-08-26 に実施**（くわしくは下の「📗 改良の記録」の 2026-08-26 の行） | 2026-08-22 自己改良中の気づき → 実施済み |
| ~~12~~ | ~~🌀 台風ページの**①②の本文に句読点の重なりがある**（`sectionActive`／`sectionTimeline`）。値が欠けている台風で「中心気圧1000hPa**、、**強さは」「中心気圧992hPa**・。**予報円」と出る~~ → **✅ 2026-08-27 に実施**（くわしくは下の「📗 改良の記録」の 2026-08-27 の行） | 2026-08-26 下書き道具の検証中に発見 → 実施済み |
| ~~13~~ | ~~🌀 `tools/typhoon_snapshot_draft.py` に、控えではなく**気象庁から直接**取ってくる `--live` を足せないか（控えが古いときでも最新で下書きが作れる。`check_typhoon_snapshot.py` に同じ通信の書き方がある）~~ → **✅ 2026-08-31 に実施**（くわしくは下の「📗 改良の記録」の 2026-08-31 の行） | 2026-08-26 自己改良中の気づき → 実施済み |
| ~~14~~ | ~~🗾 `tools/typhoon_map_coords.py` の `xy_to_lonlat()` に、SVGの `viewBox` の範囲外（地図の外）を指しているときの一言注意（「地図の外です」等）を足せないか。いまは範囲外でも計算だけして数値を返してしまう~~ → **✅ 2026-08-28 に実施**（くわしくは下の「📗 改良の記録」の 2026-08-28 の行） | 2026-08-26 候補11を実施中の気づき → 実施済み |
| ~~15~~ | ~~🌀 台風ページ①②（`sectionActive`／`sectionTimeline`）で、**気圧も風速も両方とも値が無い台風**が来ると、移動を伝える文の「。」のすぐ後に、締めの「。」だけが続いて出る可能性が残っている（実データでは今のところ未発生・気象庁は気圧はほぼ必ず出すため）。候補12の修正で仕組みは大きく直ったが、この極端なケースだけは未検証~~ → **✅ 2026-09-04 に実施**（くわしくは下の「📗 改良の記録」の 2026-09-04 の行） | 2026-08-27 候補12を実施中の気づき → 実施済み |
| ~~16~~ | ~~🗾 `tools/typhoon_map_coords.py` の順方向（緯度経度→x,y）にも、出た x,y がviewBoxの外になったときの同じ注意表示を足せないか（いまは逆方向`--xy`だけに付いている）~~ → **✅ 2026-08-29 に実施**（くわしくは下の「📗 改良の記録」の 2026-08-29 の行） | 2026-08-28 候補14を実施中の気づき → 実施済み |
| ~~17~~ | ~~🗾📚 docstringが実態とズレている箇所が2つ見つかっている。**まとめて1回で直すとよい**：①`tools/typhoon_map_coords.py` 冒頭の使用例（`24.5,132.0 → x=101.7,y=270.3`）が実際の実行結果（`x=101.4,y=278.1`）とずれている②`tools/typhoon_snapshot_draft.py` のdocstringの「（別のPRで追加予定）」が古い~~ → **✅ 2026-09-01 に実施**（くわしくは下の「📗 改良の記録」の 2026-09-01 の行） | 2026-08-29 候補16の検証中に発見／2026-08-31 候補13の検証中にもう1件発見（→旧候補20を統合）→ 実施済み |
| ~~18~~ | ~~🌀 `tools/check_typhoon_snapshot.py`（④の見張り番）も気象庁へ自前で通信している（`fetch_live_numbers()`）。候補13で `typhoon_snapshot_draft.py` は取得係の部品を借りる形にしたので、見張り番も同じように `fetch_typhoon.py` の `fetch_json()` を借りる形にそろえられないか（通信の書き方が3か所に散らばっているのを2か所に減らせる）。⚠️見張り番はワークフローから毎週動いているので、変更は慎重に~~ → **✅ 2026-09-06 に実施**（くわしくは下の「📗 改良の記録」の 2026-09-06 の行） | 2026-08-31 候補13を実施中の気づき → 実施済み |
| ~~19~~ | ~~🌀 `--live` で作った下書きには、控えから作ったときの「⚠️ この控えが◯時間前です」に当たる注意が無い。気象庁の**発表そのもの**が古い（実況が何時間も前）ケースは `--live` でも起こりうるので、実況時刻の古さを見て一言添えるとより安全かもしれない~~ → **✅ 2026-09-03 に実施**（くわしくは下の「📗 改良の記録」の 2026-09-03 の行） | 2026-08-31 候補13を実施中の気づき → 実施済み |
| ~~20~~ | ~~📚 docstringの「出力例」は手で書くと今回（候補17）のように実行結果とズレやすい。`tools/` の各道具について「docstringの出力例と実際の出力が一致しているか」を機械的に照合する小さな点検（`tools/check_docstrings.py` のような見張り番）を作れないか。まずは `typhoon_map_coords.py` の例1つだけを対象にする小さな版でよい~~ → **✅ 2026-09-02 に実施**（くわしくは下の「📗 改良の記録」の 2026-09-02 の行） | 2026-09-01 候補17を実施中の気づき → 実施済み |
| ~~22~~ | ~~🌀 候補19で追加した「実況が古い」警告は、控えから作る（`--live` なし）ときの既存の「控え自体が古い」警告と、判定の考え方（6時間しきい値・`⚠️`の言い回し）は似ているが**コードは別々**になっている。仕組みが増えてきたら共通の小さな関数にまとめられないか（急ぎではない）~~ → **✅ 2026-09-05 に実施**（くわしくは下の「📗 改良の記録」の 2026-09-05 の行） | 2026-09-03 候補19を実施中の気づき → 実施済み |
| ~~23~~ | ~~👀 `tools/check_a11y.js` を GitHub Actions で自動実行する話（候補3・4）は「Playwrightの導入」という同じ壁で止まっている。まず**ローカルまたはクラウドでPlaywrightが動くか小さく試す**（`npx playwright --version` 等）ところから始めると、次に進めるか判断しやすいかもしれない~~ → **✅ 2026-09-07 に実施**（くわしくは下の「📗 改良の記録」の 2026-09-07 の行） | 2026-09-03 候補3・4を眺めていて気づき → 実施済み |
| ~~24~~ | ~~🌀 候補15の調査中に見つけた別の小さな気づき：`sectionActive`（①発生中の台風）で、**熱帯低気圧（台風のたまご）かつ暴風域だけ値があって強風域が無い**ときだけ、文の終わりが句点「。」ではなく読点「、」で終わってしまう可能性がある（例：「暴風域は半径およそ100km、」で文が止まる）。実データでは今のところ未発生（暴風域だけ出て強風域が出ないケースが稀なため）。候補15と同じ`join()`方式に直せば解消できそう~~ → **✅ 2026-09-04 に実施**（くわしくは下の「📗 改良の記録」の 2026-09-04 の行） | 2026-09-04 候補15を実施中の気づき → 実施済み |
| ~~25~~ | ~~👀 **package.jsonの追加**：playwrightを依存関係として明示し、`npm install`で自動インストールできるようにする。GitHub Actionsのワークフローでも使える。`node_modules`と`package-lock.json`は既に.gitignoreに追加済み~~ → **✅ 2026-09-08 に実施**（くわしくは下の「📗 改良の記録」の 2026-09-08 の行） | 2026-09-07 候補23を実施中の気づき → 実施済み |
| ~~26~~ | ~~📦 package.jsonに他のscripts（`check:links`、`check:docstrings`）も追加して、npm runで統一的に実行できるようにする。GitHub Actionsでも使いやすくなる~~ → **✅ 2026-09-09 に実施**（くわしくは下の「📗 改良の記録」の 2026-09-09 の行） | 2026-09-08 候補25を実施中の気づき → 実施済み |
| ~~28~~ | ~~📚 `README.md` や `docs/` に「点検の道具は `npm run check:*` でまとめて呼べる」ことがまだ書かれていない。CLAUDE.mdの道具箱の説明に1行足すと、次のセッションが `python3 tools/...` を打ち直さずに済む~~ → **✅ 2026-09-10 に実施**（くわしくは下の「📗 改良の記録」の 2026-09-10 の行） | 2026-09-09 候補26を実施中の気づき → 実施済み |
| ~~29~~ | ~~📚 CLAUDE.md の「ディレクトリ構成」は**道具を新しく作ったときに書き足し忘れやすい**（今回3つの抜けが同時に見つかった）。`tools/` の実ファイルと構成表の行を照合する小さな点検（`npm run check:map` のような見張り番）を作れないか。まずは `tools/` だけを対象にする小さな版でよい~~ → **✅ 2026-09-11 に実施**（くわしくは下の「📗 改良の記録」の 2026-09-11 の行） | 2026-09-10 候補28を実施中の気づき → 実施済み |
| ~~31~~ | ~~🧹 済んだ候補の行には「→ ✅ 何月何日に実施。どう直して、どう確かめたか」が長く書かれているが、**同じ説明が「📗 改良の記録」にもほぼそのまま書かれている**（同じ話が2か所）。済んだ候補のほうは「→ ✅ 2026-09-04 に実施（記録を見る）」程度に短くして、詳しい話は記録の表に一本化できないか（※却下（❌）の行は理由そのものが大事なので短くしない）。アイデア帳は毎日読むので、短いほどトークンの節約にもなる~~ → **✅ 2026-09-13 に実施**（くわしくは下の「📗 改良の記録」の 2026-09-13 の行） | 2026-09-12 アイデア帳の整理中の気づき |

## 🙋 要相談（のんさんの判断待ち・勝手にやらない）

| アイデア | 理由 |
|----------|------|
| 🌀 **④予備の文章が「古い」判定のままです**（いちばん新しい実測＝2026-09-18 時点で**18.1日前**。気象庁にもう無い**台風22号・23号**が載ったままで、**いま発生中の台風25号は書かれていません**）。📏 この数字は日ごとに増えるので、**最新はいつでも `npm run check:typhoon-snapshot` で確かめられます**（この行に書いてあるのは「最後に測った日」の値です）。更新は15か所の書き換え＋内容の裏取りが要るので、**のんさんの「更新して」の一声**をお待ちします（`python3 tools/typhoon_snapshot_draft.py --live` で【1】〜【4】の下書きは自動で作れます）。⏰見張り番のワークフローはもう気づいていて、**Issue #96 が 2026-09-01 から開いたまま**です（毎週月曜7:10に見直され、直すと自動で閉じます） | 内容の正しさは人が確かめる約束のため |
| ⚙️ **点検の自動化（ワークフローの追加）を進めてよいですか**（旧候補3・4。2026-08-16／2026-08-19 から候補欄に置いたままで、**1か月動いていません**）。やることは2つ：①👀`tools/check_links.py`（公開5ページの外部リンク85本の生存確認）を月1回自動で走らせ、切れていたらIssueで知らせる ②👀`tools/check_a11y.js`（見やすさ点検）を自動で走らせる（ワークフローの中で `python3 -m http.server 8898` を1行動かす一手間が要ります。Playwrightが動くことは候補23・25で確認済み）。**「やっていいよ」の一声があれば作れます**（増えるのは `.github/workflows/` の新しいファイルだけで、既存の自動実行には触れません） | `.github/workflows/` の追加・変更は `/kaizen` の安全柵で禁止しているため（自動実行はのんさんの持ち物） |

## 📗 改良の記録

| 日付 | やったこと | PR |
|------|-----------|-----|
| 2026-08-09 | 仕組みを新設（この帳面＋/kaizenスキル＋毎朝6時のRoutine） | #36 |
| 2026-08-09 | 【1回目】CLAUDE.mdの公開範囲の記述を実態に合わせて修正（`bousai-app/` を追記。pages-deploy.yml の実際の配置手順で確認済み） | #36 |
| 2026-08-10 | 【🚀お任せモード初回】/omakaseスキル新設＋候補5件を一括消化：docs/README.md目次を全面改訂（候補1・2）／トラブル記録にトップ3早見表（候補4）／手順書にbousai-app読み替え（候補5）／スマホ点検（候補3）→両ページのタップ領域を44px化（防災＝更新ボタン・タブ・リンク一覧、台風＝タブ）。点検で見つけた残りの気づきは上の候補に補充 | #39 |
| 2026-08-10 | 【🚀お任せモード2回目・テーマ「ローカルとネット共有」】①使い分けガイドに「🌐 3つ目の場所：ネット共有」の章を新設（公開URL一覧・共有のしかた・ローカル保存との違い表）＋古い記述を更新 ②台風・防災ページのフッターに「🌐 公開ページ（いつでも最新）」への橋渡しリンク追加 ③候補1消化：台風ページのバッジ/チップ文字を0.8remに ④候補2消化：両ページにスマホ幅@media安全網 | #42 |
| 2026-08-10 | 【🚀お任せモード3回目・テーマ「外部AI連携と見える化」】①/ai-rengoスキル新設（Codex・Gemini・Ollamaとの連携の型。無い環境では/bunsanに切替）②指示書_外部AI連携セットアップ.md新設 ③tools/shinka_metrics.py＋docs/進化レポート.md新設（🔁グラフのループ＝改良のたびに数字とグラフを自動更新。/kaizen・/omakaseに組み込み済み）④CLAUDE.mdの地図更新 | #42 |
| 2026-08-10 | 【🧠記憶の同期】ローカルClaudeの個人メモリをクラウドと共有・同期する仕組みを新設：docs/共有メモリ.md（記憶の本体・毎セッション読む）＋指示書_記憶の同期セットアップ.md（@取り込み＋Obsidian Gitプラグインで10分おき自動同期）＋CLAUDE.mdに毎セッション読むルールを追記 | #42 |
| 2026-08-10 | 【🔄開始時の自動同期】セッション開始フックを改良：🧠共有メモリを読む合図を毎回表示＋ローカルでは開始時に自動でGitHubの最新を確認（mainブランチ・保存忘れなしなら自動取り込み、それ以外は「最新にして」の案内。オフラインでも正直に表示） | #43 |
| 2026-08-10 | 【台風情報改良】スマホ点検の残り候補2件を消化（#42と同内容を独立に実施→マージ時に統合）：①台風ページの小さいラベル文字を拡大②両ページにスマホ幅の`@media`安全網（480px＝#42版と420px＝#40版を共存） | #40 |
| 2026-08-10 | 【レビュー対応】PR #40への自動レビュー（Codex）の指摘2件を修正：①マップタブの予備（静的地図）が8/1の13号のまま→15号版（8/12に北陸の沖・日本海へ）に描き直して「今の状況」と同期 ②予備文章の時刻を「実況9時・発表10時ごろ」に正確化＋情報源リンクを15号向けに差し替え（実在確認済み） | #44 |
| 2026-08-15 | 【🚀お任せモード4回目・テーマ「交通・ライブカメラ」】助手2体（sonnet）で点検→安全な改善を一括実施：①カメラ一覧を20台ずつ＋路線ごとの区切りに（6.9画面→2.3画面）②現在地ボタンの表示ズレを修正（一覧と開くカメラが食い違う不具合を再現→修正）③読み上げ・キーボード操作に対応（aria-live／ボタン名／ダイアログのフォーカス）④地図に縮尺バー⑤ピンを32pxに＋枠のかたちで種類を区別（色だけに頼らない）⑥フッターのリンクの重なりを解消⑦台帳：通信エラーの取りこぼし修正・情報源ごとの安全装置・同一座標の警告・route コマンド追加・使用例の修正⑧水の上のカメラにも市区町村名（3,402→3,419台） | #52 |
| 2026-08-10 | 【台風情報改良・改善ループ】助手3体（sonnet）で点検→安全な改善を一括実施：①キキクルのリンク切れ修正（`/bosai/kizu/`→`/bosai/risk/`・404を実測確認）②予備の手書き文章を8/10の気象庁発表に全面更新（台風13・15・16号）③両ワークフローに `timeout-minutes: 10` ④スクリプトのコメント・エラーメッセージ整理⑤手順書に差分判定の補足⑥CLAUDE.mdの番号ズレ（③→④）修正。残った案は上の候補へ | #40 |
| 2026-08-16 | 【🎬YouTube型改訂】要相談欄にあった「型と実動画のズレ」を、のんさんの「お任せ」指示で解消：ガイド・テンプレ・ライブラリを実動画準拠に全面改訂（ずんだもん×四国めたんの掛け合い・容疑者3〜5人＋シロ／クロ判決・「出動」CTA・キャラ立ち絵サムネ・本題まで45秒以内の導入短縮目標） | #57 |
| 2026-08-17 | 【🚀お任せモード5回目】助手3体（sonnet）で手分け→**本体で1件ずつ検品**して候補3件を消化：①🛟防災ページに「どれくらい前の情報か」表示を移植（旧候補1。未取得のときは表示しない安全ガードつき。時刻を偽装した5パターンのテスト全合格）②🚗交通ページのカメラ探索を高速化（旧候補2。実測 231.7ms→81.8ms・場面により最大76倍速。**選ばれるカメラ・順番・距離が10パターンで完全一致**することを実データ3,424台で実証）③🚗出典の行内リンクを押しやすく（旧候補3。15px→26px・文字の高さは不変）④🌍世界カメラページを初点検してしぼりこみボタンを35px→44pxに ⑤👀点検の道具を強化（世界カメラを対象に追加／1ページだけ測る `ONLY=` /**実際に押せる範囲を測る機能**＝透明な当たり判定の工夫を正しく評価できるように）⑥🧹.gitignoreにnode_modules ⑦🛠T21を記録。**助手が見逃したT18の再発（空の黄色い箱）をスクショの目視で発見・修正** | #73 |
| 2026-08-16 | 【👀見やすさの実測】旧候補2を消化：台風・防災ページのタブの色を実測 → **10.96〜16.56:1**（必要4.5:1）で全部合格、幅320px・280pxでも横のはみ出し0pxを確認。あわせて**測り方を道具として残した**（`tools/check_a11y.js`＝色の濃さ・押しやすさ・狭い画面の3点を実ブラウザで点検）。その道具が見つけた実物件として、地図の**交通量の丸の押しやすさを改善**（見た目15pxのまま、押せる範囲を14px→18〜24pxへ。実測で前後比較。混み具合で変わる） | #63 |
| 2026-08-17 | 【🌱毎日の自己改良】候補5を消化：台風ページのタブ文字を **13.12px→14.4px**（防災の14.72pxに近づけた）。`tools/check_a11y.js`で幅320px・280pxとも横のはみ出し0pxを確認してから反映（5タブ・長めのラベルなので防災の0.92remより少し控えめな0.9remを採用し安全側に）。実機での最終確認はのんさんにお願いしたい | #74 |
| 2026-08-19 | 【🚀お任せモード6回目】助手5体（sonnet）で手分け→**本体で1件ずつ検品**して6件：①🛟防災ページにタブごとの「情報の古さ」（旧候補4。未取得なら出さない安全ガードつき・本物のデータで4タブとも確認）②🚗交通量の丸の**押し間違いを実測で発見して解消**（旧候補3。38,976点を押して確かめ、1,198点(15.8%)→11点(0.2%)。⚠️**助手の実装をいったん疑って前後比較したら「面積が減った」と誤判定しかけ、網の目で測り直して採用に転じた**）③👀`tools/check_links.py` 新設＋**リンク切れ3本を発見・修正**（ノルウェーはサービス終了・気象庁の南海トラフはURL変更・海保はトップ廃止）④📷市区町村名が付かない5台を解決（旧候補1。探す幅を1.3km→11kmに。5/5台に市区町村が付いた）⑤🇯🇵**富山県を⏸保留→❌に確定**（原文を確認。2回持ち越した宿題を解消）⑥🇨🇦BC州の高速道路は路線番号では**見分けられないと結論**（実装せず記録） | #79 |
| 2026-08-19 | 【🚗 追加改良】のんさんの「修正して」のご指示で、旧候補1の**ボロノイ方式**を実施：当たり判定を一律8px（目いっぱい）に戻したうえで、クリックを地図の層で横取りして**いちばん近い丸へ振り直す**。38,976点に本物のクリックを飛ばして実測し、**届く点 5,001→7,591（最大に回復）／押し間違い 11→0**を確認。実機相当のタッチ操作でも、旧方式が2倍遠い丸を開く場面で正しい丸が開くことを実証。キーボード操作（座標なし＝detail 0）では振り直さないので従来どおり動く | #79 |
| 2026-08-20 | 【🌱毎日の自己改良】👀`tools/check_a11y.js` の押しやすさ測定が**縦（上下）だけで、横（左右）を見ていなかった**のを修正（候補7・PR #80 を閉じるときに引き継いだ指摘）。中心から上下左右の4方向へ実際に押して測り、**縦と横の短いほう**で判定するようにした（指は丸いので、細長い部品は押しにくいため）。実際に効果あり：これまで「29px＝ほぼ十分」と報告していた丸が、**横は15px＝まったく広がっていない**と分かった（＝旧版は押しやすさを過大評価していた）。透明な当たり判定を検出できた数も 55→64個に増加。あわせて、⚠️を見て**直す必要のないものを直しかけない**ための注意書きを出力に追加（交通ページの丸は #79 の振り分け方式なので、この測り方では実際より小さく出る）。4ページとも実行して確認 | #83 |
| 2026-08-20 | 【🌱毎日の自己改良】候補5を消化：防災ページの⚡雷・☔大雨タブの「情報の古さ」表示に、2つが同じ元データを見ているため数字が同じになる旨の一言を追加。文言だけの変更で、警報判定・フォールバック（安全装置）には触れていない | #85 |
| 2026-08-22 | 【🌀台風ページの更新改良】**①更新**：④予備の文章（手書きスナップショット）が **12日前＝もう存在しない台風15号のまま**だったので、8/22 13:30の気象庁発表値に全面更新（本文・見通し・通過した台風・情報源・🗾地図の経路／凡例／説明／読み上げ文まで15か所）。地図は18号ソウデル＋20号ケナリの2本立てにし、色を「何本目か」で名付けて次回も使えるようにした。**②改良**：`tools/check_typhoon_snapshot.py`（🌀④の見張り番）＋ワークフロー「🌀 台風ページの予備文章の見張り番」（毎週月7:10・古ければIssue1枚・直れば自動で閉じる）を新設。**日付**と**気象庁の台風番号との照合**の2つで見張るので「日付だけ直して中身は古い」も捕まえられる（更新前のファイルで❌が出ることを実測して確認）。→T24 | #— |
| 2026-08-22 | 【🌱毎日の自己改良】候補10を消化：④の手描き地図の座標づくりを手計算からツールに。`tools/typhoon_map_coords.py` を新設し、`index.html` の④地図と同じメルカトル図法の式（LON0・KX・Y0・KY）で緯度経度→x,y、SVGのpath d文字列まで自動で出す。既知の都市（東京・札幌・那覇）の座標で試して、地図上の実際の点とほぼ一致することを確認。地図の描画ロジック自体（安全装置）には触れていない、単体で動く新しい道具の追加のみ | #88 |
| 2026-08-26 | 【🌱毎日の自己改良】候補9を消化：`tools/typhoon_snapshot_draft.py` を新設。④予備の文章の下書きを `data/latest.json` から作る（【1】ヘッダーの日時【2】但し書き【3】🌀発生中の台風【4】👀今後の見通し を、そのまま貼れるHTMLで出力）。**ファイルは書き換えず画面に出すだけ**＝④は人が目で確かめた文章であることに意味があるため。文章は `index.html` の①②を組み立てているJSと同じ言い回しになるよう写した。**検証**：index.html からJSの関数を切り出して同じ控えで走らせ、本文7行のうち**5行が1文字も違わず一致**、残り2行の差は**JS側の句読点の重なり**（→候補12として記録）で道具側が正しい。台風ゼロ・番号ちがい・控えが古い・リポ外のファイル指定も試して確認 | #89 |
| 2026-08-26 | 【🌱毎日の自己改良】候補11を消化：`tools/typhoon_map_coords.py` に逆方向（x,y→緯度経度）の変換 `xy_to_lonlat()` を追加。`--xy` オプションで使える。東京・札幌・那覇の座標で①緯度経度→x,y→②x,y→緯度経度の往復変換を実施し、元の値とほぼ一致することを確認（順方向の計算式・定数には触れていない） | #— |
| 2026-08-27 | 【🌱毎日の自己改良】候補12を消化：台風ページ①②の本文（`sectionActive`／`sectionTimeline`）にあった句読点の重なりを修正。中心気圧・風速などの断片を配列にまとめ、**存在する項目だけ**を `join()` で区切り記号でつなぐ方式に変更（値が無い項目ぶんの区切り記号が孤立して残らないように）。`index.html` から関数を切り出し、現在の実データ（`data/latest.json`＝台風b・18号・19号）で走らせて、句読点の重なりが**0件**になったことを確認。表示ロジックの文字列組み立てのみの変更で、地図・警報判定などの安全装置には触れていない | #91 |
| 2026-08-28 | 【🌱毎日の自己改良】候補14を消化：`tools/typhoon_map_coords.py` の逆方向変換（`--xy`）に、地図のviewBoxの外を指しているときの一言注意を追加。`index.html` の `<svg id="map-svg">` と同じ範囲（-20〜350, -10〜365）を定数として持たせ、外れていれば「⚠️ 地図の外です（viewBoxの範囲外）」を数値と併記（値自体は今までどおり返す）。範囲内・範囲外それぞれの座標で実行して出し分けを確認。座標計算そのものや地図の描画ロジックには触れていない | #91 |
| 2026-08-31 | 【🌱毎日の自己改良】候補13を消化：`tools/typhoon_snapshot_draft.py` に `--live`（控えではなく**気象庁から直接**取ってくる）を追加。取得の処理は書き写さず、毎時の取得係 `typhoon-app/scripts/fetch_typhoon.py` の `fetch_json()`・`build_typhoon()` を借りて使う（同じ処理を2か所に置いて片方だけ直す事故を防ぐため）。**安全装置**：1個でも取れなかったら下書きを作らずに止まる（台風が1つ抜けた下書きは「その台風は無い」と読めて危ないため）。台風ゼロは正常として続行。にせの取得係で6パターン（1個失敗・1個None・一覧が取れない・取得係が無い・全部そろう・台風ゼロ）を試して全合格。実データでも確認：控えが95.6時間前で18号・19号のところ、`--live` は最新の22号・23号を取得。あわせて `--main` で番号が見つからないときの案内文が「控えの中に」と決め打ちだったのを、いま出ている台風名を添える言い方に修正（`--live` でも通じるように）。**この道具はファイルを1文字も書き換えない方針は維持**（④は人が目で確かめた文章であることに意味があるため） | #91 |
| 2026-08-29 | 【🌱毎日の自己改良】候補16を消化：`tools/typhoon_map_coords.py` の順方向（緯度経度→x,y）にも、逆方向`--xy`と同じ「地図の外です」注意表示を追加。viewBoxの判定ロジックを共通化し、出たx,yが範囲外のときだけ併記するようにした。範囲内・範囲外それぞれの座標で出し分けを確認し、順方向→逆方向の往復変換でも値が一致することを確認済み。計算式（LON0・KX・Y0・KY）や地図の描画ロジックには触れていない | #91 |
| 2026-09-01 | 【🌱毎日の自己改良】PR #91 に改良が5件たまったまま（レビュー待ち）だったので、新しい改良は**追加せず**アイデア帳の整理のみ実施：旧候補17・20（どちらも「docstringが実態とズレている」という同じ種類の指摘）を1つの候補にまとめた（候補17）。コードの変更は無し | #91 |
| 2026-09-01 | 【🌱毎日の自己改良・のんさんの「実行して」で6件目】候補17を消化：`tools/typhoon_map_coords.py` のdocstringの出力例（順方向・逆方向とも）を**実際に道具を走らせた結果**に置き換え（`x=101.7,y=270.3`→`x=101.4,y=278.1` など）、`tools/typhoon_snapshot_draft.py` の「（別のPRで追加予定）」という古い注記を直した。説明文だけの変更で、計算・動作は1文字も変えていない。変更後に両方の道具を実行して動作と表示を確認 | #91 |
| 2026-09-02 | 【🌱毎日の自己改良】候補20を消化：`tools/check_docstrings.py` を新設。`typhoon_map_coords.py` の使用例（順方向・逆方向、計4行）を実際に実行し、docstringに書かれた出力例と一致するか機械的に照合する道具。ズレがあれば❌＋実際の出力を表示（終了コード1）。わざと期待値をズラして❌になることも確認済み。既存の道具・アプリのロジックには一切触れていない、単体で動く新しい見張り番の追加のみ | #— |
| 2026-09-03 | 【🌱毎日の自己改良】候補19を消化：`tools/typhoon_snapshot_draft.py` の `--live` に、気象庁の**実況時刻そのもの**が古いときの一言注意を追加。「取ってきた時刻」は新しくても「発表の中身」が古いケースを見落とさないため、発生中の台風の実況時刻のうちいちばん新しいものが6時間より前なら「⚠️ 気象庁の実況そのものが◯時間前のものです」と表示するようにした（控えから作るときの既存の古さ判定とは別に、`--live`のときだけ働く）。にせの取得係で①実況9時間前（警告あり）②実況1時間前（警告なし）を確認。控えから作る通常モードは変更前と同じ表示（実データで確認）。安全装置（1個でも取れなかったら止める）には触れていない | #97 |
| 2026-09-04 | 【🌱毎日の自己改良】候補15を消化：台風ページ②「今後の見通し」（`sectionTimeline`）に残っていた句読点の重なりバグを修正。気圧・風速・予報円・暴風域が**全部無い**予報点があると「を北へ時速20kmで進行中**。。**」のように句点が二重に出ていた（候補12では①`sectionActive`しか直っておらず、②`sectionTimeline`は未対応だった）。候補12と同じ「値がある項目だけ配列に集めて`join()`でつなぐ」方式に直し、Node.jsで4パターン（両方なし・気圧のみ・暴風域のみ・全部あり）を再現テストして、「両方なし」だけ直り、他の3パターンは表示が変わらないことを確認。地図・警報判定などの安全装置には触れていない | #97 |
| 2026-09-04 | 【🌱毎日の自己改良・のんさんの「実施して」で4件目】候補24を消化：台風ページ①「発生中の台風」（`sectionActive`）で、暴風域だけ値があって強風域が無いと文末が「暴風域は半径およそ100km、」のように読点で止まってしまう不具合を修正。調べたところ候補24の想定（熱帯低気圧だけ）より広く、**台風でも同じ不具合が起きる**と判明。暴風域・強風域の文言を配列に集めて`join('、')`でつなぎ、最後に1回だけ「です。」を付ける方式に統一。Node.jsで4パターン（両方あり・暴風域のみ・強風域のみ・両方なし）を再現テストし、「暴風域のみ」だけ読点終わりが直り、他は変化なしを確認 | #97 |
| 2026-09-06 | 【🌱毎日の自己改良】候補18を消化：`tools/check_typhoon_snapshot.py`（④の見張り番）の自前の`urllib`通信を、毎時の取得係 `fetch_typhoon.py` の `fetch_json()` を借りる形に置き換えた（`typhoon_snapshot_draft.py`の`--live`と同じやり方）。通信の書き方が3か所→2か所に集約。ワークフロー本体は変更なし。`--offline`／通常モードの両方を実行し、変更前と同じ挙動（④の古さ判定・気象庁の台風番号との照合）を確認。あわせて要相談欄の④の古さ情報を最新（22・23号→24号・7.1日前）に更新 | #— |
| 2026-09-05 | 【🌱毎日の自己改良】候補22を消化：`tools/typhoon_snapshot_draft.py` にあった2つの「古さ」警告（控え自体が古いとき／`--live`の実況そのものが古いとき）を、`stale_note()` という1つの共通関数にまとめた。しきい値6時間は `STALE_HOURS` という定数に。**表示文言は1文字も変えていない**：①控えが74.3時間前（実データ）②にせの取得係で実況が9時間前（警告あり）③実況が1時間前（警告なし）の3パターンを、変更前後で見比べて一致することを確認。安全装置（1個でも取れなかったら下書きを作らない等）や他の道具には触れていない | #97 |
| 2026-09-07 | 【🌱毎日の自己改良】候補23を消化：クラウド環境でPlaywrightが動くか確認（候補3・4のワークフロー化の下調べ）。Playwright v1.56.1が動作し、`npm install playwright`後に`tools/check_a11y.js`も正常に実行できることを確認（ローカルサーバーは別途必要だが、Playwright自体の問題はない）。GitHub Actionsで使うには`npm install playwright`のステップが必要。CLAUDE.mdの「Pre-installed browser」はChromium本体の話で、Node.jsのplaywrightパッケージは別途インストールが必要と判明。新しい候補25（package.jsonの追加）をバックログに追記。既存のアプリ・道具のコードには触れず、バックログの更新のみ | #99 |
| 2026-09-08 | 【🌱毎日の自己改良】候補25を消化：package.jsonを新規作成し、playwright ^1.56.0をdevDependenciesに追加。`npm install`でplaywrightが正常にインストールされることを確認（v1.63.0が導入された）。scriptsに`check:a11y`を追加して`npm run check:a11y`で実行できるようにした。新しい候補26（他のscriptsの追加）をバックログに追記。既存のアプリや道具には触れていない、依存関係の明示のみ | #99 |
| 2026-09-09 | 【🌱毎日の自己改良】候補26を消化：`package.json` の `scripts` に見張り番4つ（`check:links`／`check:docstrings`／`check:offices`／`check:typhoon-snapshot`）を登録し、通信のいらない2つをまとめて走らせる `npm run check` も追加。長いコマンドを覚えなくても `npm run check` の一言で点検できる。**登録前に4つとも直接実行して動作を確認**したうえで、`npm run` 経由でも同じ結果になること・`-- --offline` のような追加指定も渡せることを確認。道具そのもの（`tools/`）とワークフローには触れていない。あわせて要相談欄の④の古さを最新（10.1日前・気象庁は発生中の台風なし）に更新 | #99 |
| 2026-09-10 | 【🌱毎日の自己改良】候補28を消化：CLAUDE.md に**点検の道具の呼び方の早見表**を追加（`npm run check` ほか6コマンドを「何をするか／通信の有無」つきで一覧に。`npm run check` へ時間のかかる2つを入れていない理由も明記）。あわせて**ディレクトリ構成の抜けを補修**：作ってあるのに地図に載っていなかった `tools/typhoon_map_coords.py`・`tools/check_docstrings.py`・ルートの `package.json` の3行を追記。ドキュメントだけの変更で、道具・アプリ・ワークフローには1文字も触れていない。変更前に `npm run check` を実際に走らせ、表に書いた内容（📚出力例4行と🗾予報区58件が両方✅）が実際の出力と合っていることを確認 | #99 |
| 2026-09-11 | 【🌱毎日の自己改良】候補29を消化：`tools/check_map.py`（🗺 地図の見張り番）を新設。CLAUDE.mdの「ディレクトリ構成」の `tools/` ブロックと、実際の `tools/` の中身を突き合わせ、**書き足し忘れ・消し忘れの両方**を知らせる。`npm run check:map` で呼べるように登録し、通信のいらない `npm run check` にも同居させた（📚出力例・🗾予報区・🗺構成表の3点セット）。検知できることを2通りで実証（①作りたての自分自身が地図に無い状態 ②わざと存在しない行を足した状態、どちらも正しく❌＋終了コード1）。CLAUDE.mdの構成表と早見表にも新しい道具の行を追記。⚠️アプリのロジック・`.github/workflows/`・`data/` には1文字も触れていない | #99 |
| 2026-09-12 | 【🌱毎日の自己改良】PR #99 に改良が5件たまっている（レビュー待ち）ため、型どおり新しい改良は追加せず**アイデア帳の整理**を実施：候補表30行のうち**生きている候補は7行だけ**で、済んだ23行に埋もれて読みにくくなっていたので、済んだ・却下した候補を「📦 済んだ候補・却下した候補」へ番号順に移した（**1行も消さず、並べ替えただけ**＝旧30行と新30行が完全一致することを機械照合で確認）。あわせて候補27の「`npm run check` は2つ」という記述が候補29（🗺構成表の見張り番）の追加で**3つ**に変わっていたのを実物（`package.json`）で確かめて直し、「使い方」に移し替えの運用ルールを明記し、新候補31を追加。コード・アプリ・ワークフローには一切触れていない | #99 |
| 2026-09-13 | 【🌱毎日の自己改良】PR #99 に改良が5件たまっている（レビュー待ち）ため、新しい改良は**追加せず**アイデア帳の整理のみ実施：候補31を消化した。📦に移した済み候補22行から、長い実施レポート（同じ話が「📗 改良の記録」にもほぼそのまま書いてあった）を外し、「→ ✅ 日付に実施（記録の◯月◯日の行を見る）」の1行案内に統一。**提案の中身（どんなアイデアだったか）は1文字も消していない**（同じ提案がまた出たときに見せるため）。❌却下の行（候補2）は理由そのものが大事なので**手つかず**。機械照合で①番号の並びが変わっていないこと②案内した22件の日付がすべて記録の表に実在することを確認し、1件だけ道しるべの弱かった行（2026-08-20に2件あって候補番号の記載が無かった）に「候補7」を補った。アイデア帳は毎日読むファイルなので、**📦の部分が 10,122字→6,109字（約4割減）・ファイル全体では 24,582字→21,205字**はそのままトークンの節約になる | #99 |
| 2026-09-14 | 【🌱毎日の自己改良】PR #99 に改良が5件たまっている（レビュー待ち）ため、型どおり新しい改良は**追加せず**アイデア帳の整理のみ実施：**🙋要相談欄の「④予備の文章が古い」情報が5日前のままだった**ので、`python3 tools/check_typhoon_snapshot.py` を実際に走らせて最新の数字（10.1日前→**15.1日前**）に更新し、「⏰次の月曜にIssueを出す予定」という**もう起きたことの予定形**を「**Issue #96 が 2026-09-01 から開いたまま**」という事実の書き方に直した（GitHubで実在を確認）。あわせて「📗 改良の記録」の表の途中に空行が1つ入っていて**表が2つに分かれて表示されていた**のを詰めた。コード・アプリ・ワークフローには一切触れていない | #99 |
| 2026-09-15 | 【🌱毎日の自己改良】PR #99 に改良が5件たまっている（レビュー待ち）ため、型どおり新しい改良は**追加せず**アイデア帳の整理のみ実施：**生きている候補の記述が実態より古くなっていないか**を見直し、2行を最新化した。①**候補4**——「Playwrightの導入が要るので要検討」という足止めの理由が、候補23（2026-09-07・クラウドでPlaywrightが動くと実測）と候補25（2026-09-08・`package.json` に明記）で**すでに解消済み**だったので、いま本当に残っている宿題（ワークフローの中で `python3 -m http.server 8898` を動かす一手間／ワークフロー追加はのんさんの判断が要る）に書き替えた。②**候補32**——「📦の行が指す日付の記録に候補番号が書いてあるか」を実際に手で照合したところ**いまはズレ0件**（22行すべて命中）と分かったので、その事実を添えた（＝作るなら回帰防止の見張り番という位置づけがはっきりした）。コード・アプリ・ワークフローには一切触れていない | #99 |
| 2026-09-16 | 【🌱毎日の自己改良】PR #99 に改良が5件たまっている（レビュー待ち）ため、型どおり新しい改良は**追加せず**アイデア帳の整理のみ実施：**候補3・4が「のんさんの判断が要る」と書かれたまま1か月「💡候補」に置きっぱなし**だったのを、判断をお願いする形にまとめて**🙋要相談欄へ移した**。この2つはワークフロー（`.github/workflows/`）の追加なので、/kaizen の安全柵により**Routineがどう頑張っても着手できない**＝候補欄にあると毎朝えらび直しては毎朝見送る（実際1か月そうなっていた）。移すにあたり「はい／いいえ」で答えられるよう、やること2つ・残っている手間・触らない範囲を書き添えた。あわせて「使い方」に**判断待ちの候補は要相談へ移す**運用ルールを明記し、要相談欄の④の数字を実測で更新（15.1日前→**17.1日前**。9/14は「発生中の台風なし」だったが**いまは台風25号が発生中**＝④のズレが実害のある場面に変わった）。コード・アプリ・ワークフローには一切触れていない | #99 |
| 2026-09-17 | 【🌱毎日の自己改良】PR #99 に改良が5件たまっている（レビュー待ち）ため、型どおり新しい改良は**追加せず**アイデア帳の整理のみ実施：**🙋要相談の「④が古い」行から、毎日ズレていく数字を書き写す手作業をなくした**。この行の「◯日前」は放っておくと毎日1日ずつ嘘になるので、9/14・9/16 と2回、朝の整理がその書き替えだけで終わっていた。そこで「**最後に測った日つきの値＋最新はいつでも `npm run check:typhoon-snapshot` で確かめられる**」書き方に変え、数字そのものも実測で最新化した（17.1日前→**18.1日前**・いま発生中は**台風25号**で④には未記載）。あわせて同じ気づきを候補33として記録。通信のいらない点検3つ（`npm run check`＝📚出力例・🗾予報区・🗺構成表）を走らせて全部✅であることも確認。コード・アプリ・`.github/workflows/`・`data/` には1文字も触れていない | #99 |
