# 📝 作業ログ / 引き継ぎ書 — 2026-06-21

> このセッションでやったことの記録です。**新しいセッションで続きをやるとき**は、このログと
> [`../CLAUDE.md`](../CLAUDE.md) を読めば文脈をすぐ把握できます。

---

## 🎯 このセッションのゴール
1. YouTube動画制作の「型」を、実チャンネル「選び方特捜部」の仕様に合わせて整備
2. 非エンジニア向けに Git/Claude の使い方ガイド（スライド＋指示書）を作成
3. 成果物を `main` に反映し、ローカルへ持ち出せる状態にする

---

## ✅ やったこと（時系列）

### 1. YouTube制作テンプレの作成・改訂
- `youtube/` に企画〜公開チェックまでの一気通貫テンプレを作成。
- 当初は一般セオリーで作成 → **チャンネルのAbout情報（スクショ共有）を反映**して実態に改訂：
  - ジャンル＝DIY・農業（家庭菜園）・水回り・暮らしまわり
  - 選び方は**「3つのポイント」固定**
  - 🎤 VOICEVOX「玄野武宏」ナレーション前提（人物登場なし）
  - 🤖 AI生成画像前提（撮影B-roll欄→AI画像/図解指示欄）
  - ⚠️ 概要欄に免責・アフィリ表記・クレジット・CTA「これ気になってた！」を標準搭載
  - `library/概要欄定型文.md` を新規追加
- **PR #1**（squashマージ済み, commit `8331b64`）

### 2. Notion運用ボード → 取りやめ
- Notion書き込みがこのリモートセッションでは「requires approval」で確定できず作成不可。
- ユーザー判断で **GitHubのみで運用**に決定（手動Notion化はImport→Markdownで可能）。

### 3. Git/Claude 使い方ガイドの作成
- `docs/` にスライド＋指示書2種を作成：
  - `slides_git_claude入門.md`（Marp記法・3部構成）
  - `指示書_Claudeへの指示テンプレ.md`
  - `指示書_Git操作手順書.md`
- **Gammaスライド**も生成：https://gamma.app/docs/6k77kjnsufbh7nv
- **PR #6**（squashマージ済み, commit `e0166f2`）

### 4. 引き継ぎ整備（このコミット）
- `CLAUDE.md`（新規セッションが自動で読む案内）と本作業ログを追加。

---

## 📦 成果物の場所
| 成果物 | 場所 |
|--------|------|
| YouTubeテンプレ | `youtube/`（main） |
| 使い方ガイド | `docs/`（main） |
| Gammaスライド | https://gamma.app/docs/6k77kjnsufbh7nv（Export でPDF/PPTX可） |

---

## 🔜 次にやれること（候補）
- [ ] YouTube「選び方特捜部」の**最初の1本を企画**（`youtube/templates/01_企画書.md` から）
- [ ] 実動画を見て `youtube/00_スタイルガイド.md` 末尾の**チューニング欄**を埋める（要 `youtube.com` ネットワーク許可）
- [ ] サムネの実デザイン確認 → `youtube/templates/04_サムネイル設計.md` に反映
- [ ] Gammaスライドのテーマ・文言調整（Gamma上で編集）

---

## 🖥 ローカルへの落とし方（再掲）
```bash
git clone https://github.com/non-x2/non-x2.git   # 初回
cd non-x2
git pull origin main                              # 2回目以降は最新化
```
- 履歴不要なら GitHub の「Code → Download ZIP」でもOK。
- Gammaは https://gamma.app/docs/6k77kjnsufbh7nv → Export（PDF/PPTX/PNG）。

## ▶️ 新しいセッションで続ける手順
1. **Web**：claude.ai/code でこのリポジトリ（`non-x2/non-x2`）を選んで新規セッション。
2. **ローカルCLI**：上記でcloneしたフォルダで `claude` を起動。
3. どちらも開始時に `CLAUDE.md` を自動で読むので、「作業ログの続きから」と伝えればOK。
