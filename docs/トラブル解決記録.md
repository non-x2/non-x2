# 🛠 トラブル解決記録（再発防止メモ）

> このリポジトリの作業中に**実際に起きたトラブルと、その解決策**をまとめた「転ばぬ先の杖」です。
> 新しいセッションは [`../CLAUDE.md`](../CLAUDE.md) からここに来ます。**作業前にざっと目を通せば、同じ失敗を避けられます。**
>
> 各項目は「**症状 → 原因 → 解決 → 再発防止**」の順。新しいトラブルを解決したら、ここに1件追記してください。
> 最終更新：2026-07-19

---

## 一覧（早見表）

| # | こんな症状が出たら | ひとことで言うと |
|---|------------------|-----------------|
| [T1](#t1-notionに書き込めない) | Notionに書き込めない／「requires approval」 | このリポジトリは **GitHubのみで運用**（Notionは手動Import） |
| [T2](#t2-作業ブランチが毎回mainから分岐してしまう) | 作業ブランチがmainから分岐／差分がぐちゃぐちゃ | **作業のたびに main から新ブランチを切る** |
| [T3](#t3-git-reset---hard-が拒否される) | `git reset --hard` が拒否される | 環境のセーフガード。**そもそも使わない設計にする**（→T2） |
| [T4](#t4-prマージで-base-branch-was-modified) | PRマージで「Base branch was modified」 | **PR状態を取り直して、もう一度マージ** |
| [T5](#t5-gammaのスライドが思った形にならない) | Gammaが10枚で打ち切り／縦型にならない | **numCards ≤ 10**、縦型は **style に "vertical 9:16"** |
| [T6](#t6-youtubeの実動画を分析できない) | YouTubeの実動画を見て分析できない | ✅ **解決済み**（2026-07-19、現環境は接続OK） |
| [T7](#t7-gh-コマンドが使えない環境がある) | `gh` コマンドが使えない | 環境により不可。**GitHub連携ツール（MCP）を使う** |
| [T8](#t8-クラウドでpythonのツールが動かない) | クラウドでPythonツールが動かない（No module named…） | ✅ **解決済み**。開始フックが部品を自動導入 |
| [T9](#t9-設定ファイルの書き込みがブロックされる) | `.claude/settings.json` の作成がブロックされる | 安全装置。**設定スキル経由＋許可は読み取り専用に絞る** |

---

## T1. Notionに書き込めない

- **症状**：Notionに運用ボードを作ろうとすると「requires approval」で止まり、書き込みが確定できない。
- **原因**：リモート（クラウド）セッションからのNotion書き込みは承認が通らないことがある。
- **解決**：このリポジトリは **GitHub だけで運用**する方針に決定（PR #1 / `docs/作業ログ_2026-06-21.md`）。Notionで見たい場合は、GitHub上のMarkdownを **Notionに手動Import** すればOK。
- **再発防止**：「Notionボードで管理しよう」と新たに提案しない。資料はMarkdownで作り、必要な人が手元でNotion化する前提にする。

## T2. 作業ブランチが毎回mainから分岐してしまう

- **症状**：同じブランチを使い回していると、PRの差分に**過去にマージ済みの変更まで混ざる**／mainと食い違う。
- **原因**：このリポジトリは PR を **squash（1つにまとめて）マージ**する運用。squashすると元ブランチのコミットIDが変わるため、使い回し（rolling）ブランチは毎回 main から枝分かれしてしまう。
- **解決**：**作業（バッチ）ごとに main から新しいブランチを切る**。
  ```bash
  git checkout main
  git pull origin main                       # 最新のmainにする
  git checkout -b claude/＜今回の内容＞ main   # mainから新しい枝を作る
  ```
- **再発防止**：1テーマ＝1ブランチ。マージが終わった枝は再利用しない。ブランチ名は内容が分かる名前にする（例：`claude/ff5-anime-ep17-20`）。

## T3. `git reset --hard` が拒否される

- **症状**：`git reset --hard` を実行しようとすると、環境のセーフガードに**拒否される**。
- **原因**：作業環境が、戻せない破壊的操作を安全のためブロックしている。
- **解決**：そもそも `reset --hard` が要らない進め方にする。**T2のとおり main から新しい枝を切れば**、ブランチ同期のためのresetは不要。
- **再発防止**：「いったん全部巻き戻す」発想に頼らない。やり直したいときは**新しい枝を切り直す**。間違いを取り消したいときは Claude に「直前の操作を確認して。やり直せる？」と聞く。

## T4. PRマージで「Base branch was modified」

- **症状**：PRをマージしようとすると `Base branch was modified` と出て一度失敗する。
- **原因**：マージ直前に main 側が更新され、PRが見ていたbase（土台）と食い違ったため。
- **解決**：**PRの状態を取り直して、もう一度マージする**とたいてい成功する。慌てず再試行でOK。
- **再発防止**：複数PRを続けてマージするときは、1つマージしたら次のPRを**最新のmainで取り直して**から進める。

## T5. Gammaのスライドが思った形にならない

- **症状**：Gammaのスライドが**10枚で打ち切り**になる／**9:16の縦型**にならず横長(fluid)になる。
- **原因**：Gammaの presentation 形式の制約。
- **解決**：
  - 枚数は **`numCards` を10以下**にする（10枚を超える構成は分割する）。
  - 縦型にしたいときは **`imageOptions.style` に "vertical 9:16"** を入れる（カード自体は fluid になる前提で、画像を縦構図に寄せる）。
- **再発防止**：縦型ショート用の絵コンテは最初から「10枚以内＋縦構図プロンプト」で設計する。既存Gammaの管理IDは `ff5-anime-HANDOFF.md` §4 に控えがある。

## T6. YouTubeの実動画を分析できない 【✅ 2026-07-19 解決】

- **症状**：実チャンネルの動画を見て尺・テロップ・サムネを分析しようとすると、ページにアクセスできない。
- **原因**：リモートセッションは**ネットワーク許可リスト**で外部アクセスが制限されており、`youtube.com` 系が未許可だと開けない。
- **解決**：**2026-07-19 のセッションで `youtube.com` に接続できることを確認**し、実動画3本の分析まで完了（結果は `youtube/00_スタイルガイド.md` 末尾のチューニング欄）。
- **再発防止**：環境を作り直すと未許可に戻る可能性がある。実動画分析の前に「youtube.com につながるか確認して」とひとこと頼めばOK。つながらない間は、型づくりなどネット参照不要の作業を先に進める。

## T7. `gh` コマンドが使えない環境がある

- **症状**：`gh pr create` などの `gh`（GitHub CLI）コマンドが実行できない。
- **原因**：セッションの実行環境によっては `gh` / `hub` CLI や直接のGitHub APIが**使えない**。`ff5-anime-HANDOFF.md` には「ghが確実」とあるが、それは別環境での話。
- **解決**：GitHub連携（PR作成・コメント・CI確認など）は、その環境で提供される **GitHub連携ツール（MCPツール）**を使う。Claudeへは「**PRをドラフトで作って**」のように頼めばよく、ツールの違いはClaude側で吸収する。
- **再発防止**：手順書のコマンド例（`gh ...`）は**あくまで一例**と捉える。`gh` が無い環境では同等の操作をMCPツールで行う。Git操作の頼み方は [`指示書_Git操作手順書.md`](指示書_Git操作手順書.md) を参照。

## T8. クラウドでPythonのツールが動かない 【✅ 2026-07-28 解決】

- **症状**：クラウドで `excel-jan-barcode` のツールを動かすと `ModuleNotFoundError: No module named 'barcode'` で止まる。「これはローカル専用」と諦めていた。
- **原因**：クラウドの部屋は**毎回まっさら**なので、Pythonの部品（ライブラリ）が入っていない。
- **解決**：`excel-jan-barcode/requirements.txt` に必要な部品を明記し、**セッション開始フック**（`.claude/hooks/session-start.sh`）でクラウドのときだけ自動インストールするようにした。実際にバーコード付きExcelの生成まで確認済み。
- **再発防止**：新しくPythonツールを足すときは、**必ず `requirements.txt` に部品を書く**。フックがそれを読んで自動で用意する。ローカルでは勝手に入れない設計（あなたのPCの環境を汚さないため）。手動なら `pip3 install -r excel-jan-barcode/requirements.txt`。

## T9. 設定ファイルの書き込みがブロックされる

- **症状**：`.claude/settings.json`（権限を与える設定ファイル）を作ろうとすると、安全装置に止められる。
- **原因**：「自分に権限を与える設定」を勝手に書くのを防ぐ仕組み。**正しい動作**であって、故障ではない。
- **解決**：①設定用スキル（`update-config`）を通して作業する、②許可リストは**読み取り専用のコマンドだけ**に絞る（`git status` `git log` `ls` など）。インストール系は許可リストに入れない — フックとして登録すれば許可リストは不要。
- **再発防止**：権限を広げたくなったら、まず「本当に必要か」を確認。**書き込み・削除・インストール系は許可リストに入れない**。止められたら回避せず、のんさんに理由を伝えて判断を仰ぐ。

---

## 新しいトラブルを記録するときのテンプレ

```md
## T〇. （短いタイトル）
- **症状**：何が起きたか（出たメッセージなど）。
- **原因**：なぜ起きたか。
- **解決**：どうやって直したか。
- **再発防止**：次から避けるためのルール・気をつけること。
```

> 解決できたら、上の一覧表にも1行追記しておくと、次の人がすぐ見つけられます 🙆
