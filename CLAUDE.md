# CLAUDE.md — non-x2（のんラボ）リポジトリ ガイド

> このファイルは Claude Code が**セッション開始時に自動で読む**プロジェクト案内です。
> 新しいセッションを始めたら、まずここを起点に文脈を把握して作業を続けてください。

## このリポジトリについて
- `non-x2/non-x2` は **のんラボ（個人開発ラボ）のプロフィール兼・制作物リポジトリ**。
- 「個人がAIで何ができるか」を実証する場所。コンテンツ制作の「型（テンプレート）」が中心。
- 利用者は**非エンジニア寄り**。やり取り・ドキュメントは**日本語**、専門用語はかみ砕いて説明する。

## ディレクトリ構成
```
.
├── README.md                       # のんラボ プロフィール
├── CLAUDE.md                       # ← この案内
├── index.html                      # 🌐 公開サイトの入口ページ（GitHub Pages用）
├── .github/workflows/              # ⚙️ 自動で動くもの
│   ├── typhoon-update.yml          #    毎時25分：気象庁から台風データを取得
│   ├── bousai-update.yml           #    毎時35分：気象庁から地震・津波・雷・大雨データを取得
│   ├── livecam-update.yml          #    毎週火5:10：全国のライブカメラ台帳＋交通量観測点台帳を作りなおす
│   ├── traffic-update.yml          #    毎時45分：国土交通省から全国の交通量を取得
│   ├── pages-deploy.yml            #    Pagesを自動でオンにして公開しなおす
│   ├── offices-check.yml           #    👀 予報区リストのズレを自動照合（見張り番）
│   └── branch-cleanup.yml          #    🧹 マージ済みの古いブランチを自動削除（お掃除係）
├── .claude/                        # 🤖 Claudeの設定（クラウド・ローカル共通で効く）
│   ├── settings.json               #    セッション開始フックの登録＋権限の許可リスト
│   ├── hooks/session-start.sh      #    開始時に状態表示＋クラウドなら道具を自動準備＋ローカルならGitHubの最新を自動確認
│   └── skills/                     #    💰 型（スキル）
│       ├── git-guide/              #      /git-guide （Gitを教える＋実行）
│       ├── handoff/                #      /handoff  （引き継ぎ書を作って会話を身軽にする）
│       ├── bunsan/                 #      /bunsan   （助手に手分けして任せる）
│       ├── kaizen/                 #      /kaizen   （🌱 毎日1つ小さな改良→ドラフトPR）
│       ├── omakase/                #      /omakase  （🚀 お任せで時間内に複数の改良→ドラフトPR）
│       ├── ai-rengo/               #      /ai-rengo （🤝 外部AI＝Codex・Gemini・ローカルLLMと連携）
│       └── mojiokoshi/             #      /mojiokoshi（💬 会話をそのまま文字起こしして保存）
├── youtube/                        # 🎬 YouTube動画制作の「型」（選び方特捜部）
│   ├── README.md / 00_スタイルガイド.md
│   ├── templates/ (01〜06)         # 企画→リサーチ→台本→サムネ→SEO→公開チェック
│   ├── library/                   # フック集・CTA・視聴維持率・概要欄定型文
│   ├── examples/                  # 記入済み見本
│   └── works/                     # 🎬 実際に作る動画（1本＝1フォルダ）
│       └── 2026-09_防災グッズ5選/ #    次の1本（9/1「防災の日」向け・企画書まで）
├── docs/                           # 📚 Git/Claudeの使い方ガイド＋作業ログ
│   ├── slides_git_claude入門.md
│   ├── 指示書_Claudeへの指示テンプレ.md
│   ├── 指示書_Git操作手順書.md
│   ├── トラブル解決記録.md         # 🛠 過去のトラブルと解決策（再発防止メモ）
│   ├── 手順書_台風ページの公開と自動更新.md # 📗 公開・自動更新の確認と困ったときの対処
│   ├── クラウドとローカルの使い分け.md # ☁️💻 どっちで何をやるかの地図
│   ├── 使い分けガイド_トークン節約の3つの仕組み.md # 💰 /handoff・/bunsan・Routineの使い分け
│   ├── 手順書_Obsidian活用.md       # 📓 無料ノートアプリObsidianでのんラボを読む・探す
│   ├── Claude最新情報_2026-06-26.md # 🆕 Claudeモデル＆Claude Codeの定点メモ
│   ├── 自己改良バックログ.md       # 🌱 毎日の自己改良のアイデア帳＋記録（共有の脳）
│   ├── 指示書_外部AI連携セットアップ.md # 🤝 Codex・Gemini・Ollamaの入れ方と役割分担
│   ├── 共有メモリ.md               # 🧠 ローカル⇄クラウド共通の記憶（毎セッション読む・30行以内）
│   ├── 指示書_記憶の同期セットアップ.md # 🔄 個人メモリの書き出しとObsidian Gitでの自動同期
│   ├── 進化レポート.md             # 📊 のんラボの成長を数字とグラフで（自動生成・手で編集しない）
│   └── 作業ログ_*.md               # セッションごとの作業記録（引き継ぎ用）
├── typhoon-app/                    # 🌀 台風情報まとめ（index.html 1枚・5タブ／ブラウザで直接開ける）
├── bousai-app/                     # 🛟 防災情報まとめ（地震・津波・雷・大雨／台風ページの弟分）
├── traffic-app/                    # 🚗 交通・ライブカメラ＋🚦いまの交通量
│   ├── index.html                  #    ページ本体（地図も自前・外部ライブラリ0）
│   ├── data/livecams.json          #    台帳のコピー（大もとは livecam-db/）
│   ├── data/road_status.json       #    🚦 交通量の控え（毎時45分に自動更新）
│   ├── data/road_points.json       #    🚦 交通量の観測点台帳（約1000か所・市区町村名つき）
│   └── scripts/fetch_road_status.py #   交通量を取ってくる係
├── world-livecam/                  # 🌍 世界のライブカメラ（試験・米アイオワ州＋加BC州 計1,816台を検索＋地図で）
│   ├── index.html                  #    ページ本体（外部ライブラリ0・台帳は中に埋め込み）
│   └── data/livecams_world.json    #    台帳のコピー（大もとは livecam-db/）
├── livecam-db/                     # 📷 ライブカメラ台帳（★大もと・他のアプリからも使える）
│   ├── README.md                   #    スキーマ・使い方・情報源を選ぶ約束＋🌍世界の調査記録
│   ├── build.py                    #    台帳を作る司令塔（集める→まとめる→https確認→市区町村）
│   ├── build_world.py              #    🌍 世界台帳（試験）の司令塔（日本版とは別ファイルを作る）
│   ├── export.py                   #    台帳を各アプリに配る係（日本の台帳だけを配る）
│   ├── livecam.py                  #    台帳を使う道具（近く・ルート沿い・検索／CLIあり）
│   ├── sources/                    #    情報源ごとの読み取り係（足すのはここ）
│   ├── data/livecams.json          #    できあがった台帳（大もと）
│   └── data/livecams_world.json    #    🌍 世界台帳（試験・日本のアプリには配らない）
├── data/offices.json               # 🗾 全国58予報区リストの「大もと」（両アプリ共通・1か所だけの正）
├── tools/                          # 🧰 小さな道具箱
│   ├── check_offices.py            #    👀 予報区リストのズレを照合する見張り番（手動でも実行可）
│   ├── shinka_metrics.py           #    📊 進化メトリクス集計（docs/進化レポート.md を自動生成）
│   ├── kaiwa_mojiokoshi.py         #    💬 会話の記録を読みやすい文字起こしに変換（/mojiokoshi の中身）
│   ├── check_a11y.js               #    👀 見やすさ点検（色の濃さ・押しやすさ・狭い画面のはみ出し）
│   └── check_links.py              #    👀 リンクの見張り番（公開5ページの外部リンク85本の生存確認）
├── excel-jan-barcode/              # 🏷 JANコード→バーコード付きExcel生成ツール（Python）
└── ff5-anime-*.md                  # 🎥 FINAL FANTASY V アニメ化の企画・制作キット群
```

## 進行中プロジェクトと状況
1. **YouTube「選び方特捜部」** — テンプレ完成＋**チャンネル開設・動画3本公開済み**（`@erabikata-tokusobu`：本編10:34／ミニ解説2:42／ショート0:30）。
   - 2026-07-19 に実動画を分析し、`youtube/00_スタイルガイド.md` 末尾のチューニング欄に記録済み。
   - ✅ 2026-08-16 **型を実動画に合わせて全面改訂**（ずんだもん×四国めたんの掛け合い・容疑者3〜5人でシロ／クロ判決・「出動」CTA・キャラ立ち絵サムネ・本題まで45秒以内の導入短縮目標）。旧型はGit履歴と `youtube/examples/` 冒頭の注記に記録。
   - 🎬 **次の1本の企画書あり** → `youtube/works/2026-09_防災グッズ5選/01_企画書.md`（本編9分30秒・容疑者5人・**9/1「防災の日」の直前公開ねらい**。台風／防災ページへの導線も設計済み）。⚠️ 数字は**撮影前にのんさんが一次情報で裏取り**（企画書の末尾にリストあり）。
2. **Git/Claude 使い方ガイド** — 完成・main反映済み。Gammaスライド：https://gamma.app/docs/6k77kjnsufbh7nv
3. **FF5アニメ化** — `ff5-anime-*.md` に企画・絵コンテ・SNSショートキット等（第16話まで脚本済み）。
4. **台風情報まとめ** — `typhoon-app/`。**2026-08-01 に自走化**（⚡Claudeを開かなくても中身が新しくなる）。
   - **4段構えで更新**：①ブラウザから気象庁へ直接（開くたび最新）→ ②`data/latest.json`（GitHub Actionsが毎時更新）→ ③端末に残った前回のデータ（圏外でも出る）→ ④手書きのスナップショット（予備）。上から順に試し、ダメでも必ず次が出る。
   - 開きっぱなしでも30分ごとに自動で取り直し。ヘッダーの「🔄 いま更新する」ボタンで手動更新も可。
   - 「今の状況」「マップ」は①②のときJSが**全部組み立て直す**（発生中の台風・見通しの時系列・経路図・予報円・暴風域・日本への最接近距離）。
   - 🌐 **2026-08-15 に「🌐 3D地球儀」タブを追加**（5タブに）。🗺️マップと同じ気象庁データを、指でくるくる回せる地球の上に描く（Canvas＋正射図法／外部ライブラリなし）。⏱つまみで「実況→24時間後→…」と予報の時刻を動かせる。世界の海岸線はNatural Earth 1:1000万を間引いて約6KBの文字列にしてHTML内に埋め込み、**日本の詳しい海岸線は🗺️マップのSVGを読み直して使い回す**（新しい地図ファイルは足していない）。💨渦のアニメーションは**暴風域・強風域の大きさから描いたイメージ**で、実測の風ではない旨を画面に明記。見ていないあいだは描画を止めて電池を節約。
   - 🌍 **2026-08-15 に地球儀へ「世界の台風」を追加**。海ごとに公式を分担（日本周辺＝気象庁／大西洋・東太平洋・中部太平洋＝米NHC／南半球・北インド洋＝米軍JTWC。どちらも米政府＝パブリックドメイン・出典明記）を `scripts/fetch_world.py` が毎時25分に `data/world.json` へ控え、地球儀に**紫の点線**で重ねる。**WMO・豪州気象局は規約が「個人利用のみ」なので不採用**（調査記録は作業ログ2026-08-15）。おまけレイヤー設計＝取れない・12時間より古いときは描かず正直表示、気象庁の表示には一切影響しない。安全装置はT12と同じ（1個でも失敗したら書き換えない）。
   - 🌓 **2026-08-16 に地球儀へ「昼と夜」「📈風の強さグラフ」「⚡省エネ描画」を追加**。夜側は太陽の位置から計算してうっすら暗く（境目は画面上の楕円弧として描く。半球をfillGeoで塗ると裏返ることがある＝READMEに注意書きあり）。風速グラフは台風1つ1行の折れ線（発表値のみ・補間なし・数値ラベル必須）。描画は「下絵キャッシュ＋毎コマは渦と脈動だけ」方式で60fps維持と省電力を両立。
   - ⚠️ **2026-08-05 に「⚠️ 警報・注意報」タブを追加**（当時は4タブ）。台風に関係する5種類（暴風・波浪・高潮・大雨・洪水）だけを全国**58予報区**ぶん確認して一覧表示。警報以上が出るとタブの見出しが「⚠️ 警報・注意報 🔴」に変わる。雷・乾燥などは `bousai-app/` の担当。
   - 警報は**通信を2段階**に分ける：開いた直後は②控えだけ（1回・軽い）／⚠️タブを開いたときだけ①気象庁へ直接（58回。データ量は実測407KB・実際に届くのは約34KB＝gzip。重いのは**回数**のほう）。**1つでも失敗したら全部やめて控えに切り替え**（一部だけ取れた状態は「警報なし」と誤表示して危ないため）。
   - 🛡 **古い控えで「警報なし」と断言しない**：控えが6時間以上前、または気象庁の発表が24時間以上前なら、緑の「出ていません」ではなく「確認できていません」を出す。`status` は**「解除・なし」以外はすべて“出ている”扱い**（「警報から注意報」等の格下げ表記を取りこぼさないため）。
   - 部品：`scripts/fetch_typhoon.py`（Python標準機能のみ）＋ `.github/workflows/typhoon-update.yml`（毎時25分／変化が無い回はコミットしない／1個でも取得失敗したら上書きしない安全装置つき）。
   - **予備の文章（④）だけは手書き**。直すときは「予備の文章を更新して」と頼み、冒頭の `<time datetime="...">` も更新する。
   - **公開も自動**：`.github/workflows/pages-deploy.yml` が公開を実行（mainの更新時＋データ自動更新の完了時＋手動）。**2026-08-02 に公開開始済み**。※Pagesを最初にオンにする操作だけは自動化できず（→T16）、のんさんが設定済み（Settings → Pages → Source =「GitHub Actions」）。**以後の設定作業は不要**。
   - 公開URL：https://non-x2.github.io/non-x2/typhoon-app/ ／ 入口ページ：ルートの `index.html`
   - 🔒 **Webに出すのは `index.html`・`typhoon-app/`・`bousai-app/`・`traffic-app/`・`world-livecam/` の5つだけ**（作業ログ・脚本・YouTubeの型・`livecam-db/` は公開対象外。検索に載る範囲を最小限にするため）。リポジトリ自体はpublic＝GitHub上では元から誰でも閲覧可。
   - ⚠️ 定期実行は**mainに入ってから**動く。60日放置でGitHubが自動停止（Actionsタブから再開可）。
   - 📗 確認のしかた・困ったときの対処 → [`docs/手順書_台風ページの公開と自動更新.md`](docs/手順書_台風ページの公開と自動更新.md)
5. **防災情報まとめ（地震・津波・雷・大雨）** — `bousai-app/`。台風ページと同じ4段構え（①ブラウザから気象庁へ直接→②`data/latest.json`（毎時35分に自動更新）→③端末に残った前回分→④案内文＋公式リンク）。4タブ構成（🌏地震／🌊津波／⚡雷／☔大雨）。地震・津波・雷・大雨は**別々に**フォールバックする。安全装置（1種類でも取得失敗なら上書きしない・津波が判定不能なら「確認できません」と正直表示）つき。公開URL：https://non-x2.github.io/non-x2/bousai-app/
   - 🗾 **予報区リスト（58予報区）は `data/offices.json` が「大もと」**（台風・防災の両アプリ共通。2026-08-08 に1か所に整理）。両Pythonスクリプトはここを読むだけ。各 `index.html` の埋め込みだけは「1枚で開ける」ため残してあり、ズレると `tools/check_offices.py`＋GitHub Actions「👀 予報区リストの見張り番」が知らせる。**直すときは `data/offices.json` → HTML2枚の順でそろえる**。
6. **JANバーコードExcelツール** — `excel-jan-barcode/`。完成・main反映済み（PR #18）。**クラウドでも実行可**（セッション開始フックが部品を自動導入。2026-07-28 に動作確認済み）。実機スキャナーでの読み取り確認が残課題（＝ここだけローカル）。
7. **交通・ライブカメラ** — `traffic-app/`。**2026-08-14 新設**。出発地・目的地を入れると車のルートを地図に描き、**その通り道にあるライブカメラ**を拾い出す。カメラを押すと今の様子の映像が出る（30秒ごとに自動で撮り直し）。
   - 🔑 **いちばん大事な仕組み**：カメラの写真URLはほぼ全部 `http://`。公開ページは `https://` なので、そのままだとブラウザにブロックされる（混在コンテンツ）。そこで**台帳を作るときに `https://` へ読み替えて1台ずつ実際に開いてみて**、画像が返るものだけ「ページ内で見られる」と記録する。出せないカメラも消さず、公式ページへのリンクで案内する。
   - **外部ライブラリ0・APIキー0**。地図は地理院タイルを自前で並べて描画（他ページと同じ「1枚で開ける」方式）。地名→緯度経度＝国土地理院 住所検索、ルート＝OSRM公開サーバー。
   - 🌊 **道路以外のカメラ（河川・ダム・海）はチェックボックスで出し入れ**できる（既定は道路のみ）。
   - 🛣 **高速道路は「道路」に含まれる**（🛣のしるし・紫の丸で区別）。入っているのは**国が管理する**高速・自動車専用道路のみ（81台）。**NEXCO・首都高・阪神高速は利用規約で他サイトへの転載が禁じられているため取り込まず、公式サイトへのリンクで案内**している。
   - ⚠️ **所要時間は渋滞を考えていない目安**。無料・鍵不要でブラウザから直接使える全国リアルタイム渋滞APIが無いため、渋滞の全体像は公式サイト（JARTIC等）へのリンクで案内している。
   - 🗾 **県が管理する道路のカメラは各県のサイトへのリンク**で案内（取り込んでいない理由は下の8番を参照）。
   - 🚦 **2026-08-15 に「いまの交通量」を追加**：国土交通省の交通量API（全国の高速・国道の観測点 約1,000か所、5分間の台数、約20分遅れ）を毎時45分にActionsが取得して `data/road_status.json` に控え、地図に色の丸で重ねる。**速度が無いので渋滞判定はできない**→「1週間前の同じ時刻と比べて多いか」（🔴かなり多い🟠多い🟢いつも並み🔵少ない⚪比べられない）という正直な見せ方。判定は倍率＋増加台数の両方（少ない道で率だけ跳ねるのを防止）。APIはCORS未対応＝ブラウザ直接不可（実測済み）なので控え方式一択。取得失敗時は書き換えない（T12）。控えが6時間より古いときは色分けを休む。観測点台帳（市区町村名）は毎週火5:10の台帳更新と同時に手入れ。出典表記「国土交通省API機能による交通量(参考値)を加工して作成」は削らない（利用規約の条件）。
   - 公開URL：https://non-x2.github.io/non-x2/traffic-app/
8. **📷 ライブカメラ台帳** — `livecam-db/`。**2026-08-15 新設**。カメラ情報の**大もと（1か所だけの正）**。他のアプリからも使える。
   - 部品：`build.py`（集める→重複をまとめる→https確認→市区町村を付ける）／`export.py`（各アプリに配る）／`livecam.py`（**使う道具**：近く・ルート沿い・検索。CLIとしても動く）／`sources/`（情報源ごとの読み取り係。**1ファイル足せば情報源を増やせる**）。
   - `traffic-app/data/livecams.json` と `index.html` の埋め込みは**ここから配られたコピー**。直すときは `build.py` → `export.py` の順（アプリ側を直接いじらない）。
   - ⚖️ **情報源を選ぶ約束（重要）**：公的機関が公開しているものだけを使う。**「無断転載を禁じます」と明記されている自治体サイトや、民間のまとめサイトの名簿は取り込まない**（リンクで案内する）。調べた結果と判断理由は `livecam-db/README.md` に一覧で記録済み——**同じ調査を繰り返さないこと**。
   - 📄 海上保安庁のデータは **PDL1.0（出典を書けば再利用可）**。出典の記載が条件なので `sources` と `owner` に残している。
   - ⚙️ 毎週火5:10に `.github/workflows/livecam-update.yml` が `build.py`＋`export.py` を実行（カメラの増減・故障に追従）。
   - 🌍 **世界対応（2026-08-16 開始・試験）**：「全て網羅」は世界の誰にも不可能（統一台帳なし・権利の壁・毎日増減）と正直に整理したうえで、①`traffic-app` に世界の公式サイト18リンクの案内欄 ②別台帳 `data/livecams_world.json`（`build_world.py`）に🇺🇸**アイオワ州DOT 790台**（**CC BY 4.0**）と🇨🇦**カナダBC州 DriveBC 1,026台**（**Open Government Licence – British Columbia**・2026-08-19 追加）を試験取り込み。どちらも出典明記が条件。世界分は日本のアプリに配らない（重くしないため）。③Routine「🌍 世界カメラ情報源の定期調査」が**毎月20日朝7時**に新情報源を探索（外部AIを点呼→不在ならClaude助手。発見があればドラフトPR・なければ静かに終了）。調査済みの記録は `livecam-db/README.md`——**同じ調査を繰り返さない**。
9. **🌍 世界のライブカメラ（試験）** — `world-livecam/`。**2026-08-16 新設**。8番でためた世界台帳を、実際に**見られるページ**にしたもの。
   - **なぜ別ページにしたか**：交通ページの地図は国土地理院タイル＝**日本しか映らない**ので、アイオワ州の上には何も描けない。混ぜるとページも244KB重くなる。→ 分ければ日本のページは**1バイトも重くならない**。
   - できること：名前・道路番号で検索／🛣高速・一般道でしぼりこみ／点の地図（押すとカメラが開く）／映像は30秒ごとに自動で撮り直し。**見えているカメラだけ**読み込むので通信をむだにしない（最初は48枚・「もっと見る」で追加）。
   - 🗺 **地図の絵は描いていない**：カメラの点そのものが道路の並びなので、点だけで州の形と幹線道路が見える。地図の範囲はデータから自動で決まるので、地域が増えても壊れない。
   - 🌍 **2026-08-19 に🇨🇦カナダBC州 1,026台を追加**（計1,816台）。**地域のしぼりこみボタン**（すべて／🇺🇸アイオワ州／🇨🇦BC州）を新設。検索は名前だけでなく**場所でも**引ける（例：「バンクーバー島」）。
   - 📄 **出典表記は2つとも消さないこと**（ライセンスの条件）：「Iowa Department of Transportation - Office of Traffic Operations（CC BY 4.0）」と「Contains information licensed under the Open Government Licence – British Columbia.」。
   - ⚠️ **`export.py` の圧縮データには `s`（情報源）の欄が必要**。無いと地域のしぼりこみが全部0台になる（一度そうなって実ブラウザで発見・修正した）。
   - 直すときは `livecam-db/build_world.py` → `export.py` の順（ページ側のデータを直接いじらない）。毎週火5:10の台帳更新に世界分も相乗り（失敗しても日本側は止めない）。
## のんさんへの対応の型（★どの環境でも必ず守る）

> クラウドでもローカル（デスクトップアプリ）でも**同じ対応**になるよう、ここに明記しています。

- **言語**：必ず日本語。説明は**中学生レベルの言葉**（専門用語はかみ砕く）。
- **🧠 共有メモリ**：セッション開始時に [`docs/共有メモリ.md`](docs/共有メモリ.md) も読む（ローカル⇄クラウド共通の記憶。短いので負担なし）。
- **🔒 個人の記憶（非公開）**：☁️クラウドでは、セッション開始時に非公開リポ `non-x2/kioku-sync` を `add_repo` で追加して、その中の `docs/共有メモリ.md` も**頼まれなくても自動で読む**（2026-08-10導入）。読めなかったときは正直にそう報告し、作業は普通に続ける。⚠️中身はビジネスの内側の話なので、**公開される場所（コミット・PR本文・公開ページ）には書き写さない**。
- **作業前**：全体の手順を説明してから着手する。**戻しにくい操作は必ず事前確認**（公開・削除・マージなど）。
  トークンを多く使いそうな操作（大きいファイル添付・長いコード生成・PDF処理）の前には**節約案を先に提案**。
- **作業中**：今なにをしているか日本語で実況し、進行中とわかる表示を添える（⏳🔄）。
- **作業後**：終わったら必ず報告する（✅完了しました）。**結論ファースト**。
- **会話管理**：20〜30往復、または話題が一段落したら**`/handoff` スキルを使う**（作業ログ＋15行の貼り付け用引き継ぎ文を作る）。手作業で要約せず、必ずこの型を通すこと。
- **🔄 引き継ぎを受け取ったとき（★必ず合図を出す）**：新しいチャットの最初に「**🔄 引き継ぎメモ**」が貼られたら、
  ①メモで名指しされたファイル（`docs/作業ログ_*.md` など）を読んで中身を把握し、
  ②**返事のいちばん最初の行に「✅ セッションに引き継がれました」と必ず表示する**。
  そのあとに「何を引き継いだか」を2〜3行で要約して、次にやることを確認する。
  ⚠️ 名指しのファイルが読めなかった・見つからなかったときは、**この合図を出さずに正直にそう伝える**
  （合図＝「ちゃんと引き継げた」の証明なので、読めていないのに出すと嘘になる）。
- **重い作業**：調べ物・レビューが多いときは **`/bunsan` スキル**で助手に分散する（本体のトークンを節約するため）。**使ったモデルと体数は必ず日本語で申告する**。
- 💰 使い分けの地図 → [`docs/使い分けガイド_トークン節約の3つの仕組み.md`](docs/使い分けガイド_トークン節約の3つの仕組み.md)

## 作業の進め方（このリポジトリの慣習）
- **ブランチ**：`claude/<内容>` を新規作成して作業（main直編集はしない）。
- **PR**：プッシュ後にドラフトでPR作成 → ユーザー確認 → **squashマージ**。
- **コミット末尾**に以下を付与（モデル名は**そのセッションで実際に動いているモデル**に合わせる。例：Claude Fable 5）：
  ```
  Co-Authored-By: Claude <モデル名> <noreply@anthropic.com>
  ```
- **言語**：日本語。トーンはやさしく、結論ファースト。
- **安全**：公開・削除・マージなど戻しにくい操作は事前に確認を取る。
- **トラブル対策**：過去に起きた失敗と解決策は [`docs/トラブル解決記録.md`](docs/トラブル解決記録.md) に集約。**作業前に目を通して同じ轍を踏まない**。新たに解決したトラブルは同ファイルに1件追記する。

## 続きを始めるときのヒント
- 「クラウドとローカル、どっちでやる？」→ [`docs/クラウドとローカルの使い分け.md`](docs/クラウドとローカルの使い分け.md)。
- 「マージって何？」「mainに反映して」などGit関連 → **`/git-guide` スキル**（`.claude/skills/git-guide/`。教える＋のんラボ流の型で実行）。
- 「YouTubeの次の1本を企画したい」→ `youtube/templates/01_企画書.md` から。型は `youtube/00_スタイルガイド.md`。
- 「指示の仕方を確認したい」→ `docs/指示書_Claudeへの指示テンプレ.md`。
- 「ノートをPCで快適に読みたい」「Obsidianって何？」→ [`docs/手順書_Obsidian活用.md`](docs/手順書_Obsidian活用.md)（リンクは `[[ウィキリンク]]` ではなく**普通のMarkdown形式**で書く約束）。
- 「前回の作業内容を知りたい」→ `docs/作業ログ_*.md`（最新日付）。**まずこれ1枚だけ読む**（リポジトリ全体を漁らない＝トークン節約の近道）。
- 「会話が長くなってきた」「今日はここまで」→ **`/handoff`**。
- 「この会話をそのまま残したい」「文字起こしして」→ **`/mojiokoshi`**（要約ではなく逐語で `docs/会話ログ/` に保存。⚠️プッシュすると公開されるので事前確認あり）。
- 「調べ物が多い」「レビューもしてほしい」→ **`/bunsan`**。
- 「自動で点検してほしい」→ Routine（🩺 のんラボ 月イチ健康診断・毎月1日9時）。止めたいときは「月イチ健康診断を止めて」と頼む。
- 「リンクが切れていないか調べて」→ **`python3 tools/check_links.py`**（👀リンクの見張り番）。公開5ページの外部リンクを全部たたいて、**「切れている」と「この部屋から届かない」を分けて**教える。外部リンクを足したときは必ず走らせる（→T23）。
- 「まとまった時間でお任せで改良してほしい」→ **`/omakase`**（🚀 全自動お任せモード。既定2時間で複数の改良→ドラフトPR1本）。
- 「外部AI（Codex・Gemini・ローカルLLM）と連携したい」→ **`/ai-rengo`**（🤝 点呼→相談→検品の型。無い環境では正直に言って /bunsan に切替）。入れ方は [`docs/指示書_外部AI連携セットアップ.md`](docs/指示書_外部AI連携セットアップ.md)。
- 「のんラボの成長を数字で見たい」→ [`docs/進化レポート.md`](docs/進化レポート.md)（🔁 /kaizen・/omakase が改良のたびに自動更新する「グラフのループ」）。
- 「ローカルの記憶をクラウドと共有・同期したい」→ [`docs/指示書_記憶の同期セットアップ.md`](docs/指示書_記憶の同期セットアップ.md)（🧠 共有メモリ＋Obsidian Gitで自動同期）。
- 🌱 **毎日の自己改良**：Routine「🌱 毎日の自己改良」（毎朝6時）が `docs/自己改良バックログ.md` を読んで小さな改良を1つだけ実行し、**ドラフトPRを作る**（マージはのんさん）。型は **`/kaizen`**（ローカルでも手動で使える）。前回の🌱PRが未マージの日は自動でお休み。止めたいときは「毎日の自己改良を止めて」と頼む。
