# 📷 ライブカメラ台帳（livecam-db）

日本全国のライブカメラを **1か所にまとめた「台帳（データベース）」** です。
のんラボのどのページからでも、ここを読めば同じカメラ情報が使えます。

> 🔑 **このフォルダが「大もと」です。**
> 各アプリ（`traffic-app/` など）が持っているカメラ情報は、ここから配ったコピーです。
> 直したいときは、アプリ側ではなく**ここを作りなおして配りなおして**ください。
> （`data/offices.json` と同じ「正しい情報は1か所だけ」の考え方です）

---

## いま入っているカメラ

`python3 livecam-db/livecam.py info` でいつでも確認できます（2026-08-15 時点：**3,424台**／うち
**2,572台**はページ内に映像を出せる／一般道1,331・高速81・河川1,819・ダム114・海79）。

| 情報源 | 中身 | 利用条件 |
|---|---|---|
| 国土技術研究センター（JICE）道路チェック地図 | 国土交通省 各地方整備局ほかの**道路**ライブカメラ | 国が公開している地図データ |
| 国土技術研究センター（JICE）河川チェック地図 | 同じく**河川・ダム**のライブカメラ | 同上 |
| 海上保安庁 ライブカメラ | 灯台・港・岬の**海**のライブカメラ | **公共データ利用規約（PDL1.0）**＝出典を書けば再利用可 |

種類（`cat`）は `road`（一般道）／`expressway`（高速・自動車専用道路）／`river`（河川）／
`dam`（ダム）／`weir`（堰・水門）／`sea`（海）に分かれています。

> 🛣 **高速道路について**：`expressway` は**国が管理する**高速道路・自動車専用道路です
> （三陸道・京奈和自動車道・北近畿豊岡自動車道・名阪国道など **81台**）。
> **NEXCO・首都高・阪神高速などが管理する高速道路のカメラは入っていません**——
> 各社の利用規約で他サイトへの転載が認められていないためです（下の「取り込まなかった情報源」を参照）。
> 名前で見分けているため、「◯◯道路」で終わる路線（自動車専用道路のこともある）は `road` のままです。

> 📄 **出典の記載について**：海上保安庁のデータは PDL1.0 の条件として出典の記載が必要です。
> 台帳の `sources` 欄に出典を記録し、各カメラの `owner` にも「海上保安庁 ◯◯海上保安部」と入れています。

---

## 使い方

### コマンドで調べる

```bash
python3 livecam-db/livecam.py info                                        # 台帳の要約
python3 livecam-db/livecam.py near 35.681 139.767 --radius 10000          # 東京駅の近く（6台）
python3 livecam-db/livecam.py near 34.70 137.73 --radius 10000 --category river  # 河川だけ（10台）
python3 livecam-db/livecam.py search 国道1号 --pref 静岡県                 # 名前・場所・管理者で探す
python3 livecam-db/livecam.py route 40.81,140.45 40.83,140.73                # ルート沿い（五所川原→青森・3台）
python3 livecam-db/livecam.py search 天竜川 --with-image --json           # JSONで受け取る
```

### プログラムから使う（他のプロジェクトでも同じ）

```python
import sys; sys.path.insert(0, "livecam-db")
import livecam

db = livecam.load()

# ① ある地点の近くのカメラ（近い順）
for cam in livecam.near(db, 35.681, 139.767, radius_m=10000, limit=5):
    print(cam["name"], cam["place"], round(cam["distance_m"]), "m")

# ② ルート沿いのカメラ（出発地からの順）  route = [[緯度, 経度], …]
for cam in livecam.along_route(db, route, radius_m=1500):
    print(cam["name"], "出発から", round(cam["along_m"] / 1000, 1), "km")

# ③ しぼり込み
rivers = livecam.filter_cams(db, category="river", pref="新潟県", with_image=True)
```

---

## カメラ1台の形（スキーマ）

```json
{
  "id":    "jice-roads-00042",
  "src":   "jice-roads",
  "cat":   "road",
  "name":  "国道1号",
  "place": "静岡県静岡市清水区",
  "lat":   35.012345,
  "lon":   138.123456,
  "img":   "https://…/cam.jpg",
  "page":  "https://…",
  "owner": "中部地方整備局 静岡国道事務所"
}
```

| 項目 | 意味 |
|---|---|
| `id` | 台帳の中での通し番号 |
| `src` | どの情報源から来たか |
| `cat` | 種類（road / expressway / river / dam / weir / sea） |
| `name` | 路線名・河川名など |
| `place` | 市区町村（緯度経度から自動で付けています）。**5台だけ空**＝海上や山間部で、国土地理院の区域データに当たらない地点 |
| `lat` / `lon` | 緯度・経度 |
| `img` | **ページ内にそのまま出せる写真URL**。出せないカメラは `null` |
| `page` | 公式のカメラページ |
| `owner` | 管理者（◯◯地方整備局 など） |
| `also` | 別の情報源にも載っていた場合、その情報源（**無いカメラが大半**。`cam.get("also", [])` で読むこと） |

---

## 🔑 いちばん大事な仕組み：「https で開ける写真だけ `img` に入れる」

全国のライブカメラの写真URLは、**ほとんどが `http://`（暗号化なし）** で公開されています。
一方、のんラボの公開ページは `https://` なので、`http://` の写真をそのまま出そうとすると
ブラウザが**問答無用でブロック**します（「混在コンテンツ」という安全のしくみ）。

そこで台帳を作るときに、`http://` を `https://` に読み替えて
**1台ずつ実際に開いてみて**、ちゃんと画像が返るものだけ `img` に入れています。

開けなかったカメラも**消しません**。`img` を `null` にして `page`（公式ページ）を残すので、
アプリ側は「ここにカメラはあるけれど、映像は公式ページで見てください」と案内できます。

---

## 台帳の作りなおし方

```bash
python3 livecam-db/build.py      # 集める → 重複をまとめる → 写真を確認 → 市区町村を付ける
python3 livecam-db/export.py     # 各アプリに配る（traffic-app など）
```

写真の確認は3,000台以上に1台ずつアクセスするので**15〜20分**かかります。急ぐときは:

```bash
python3 livecam-db/build.py --no-verify     # 写真の確認をとばす（前回の結果を使う）
python3 livecam-db/build.py --no-geocode    # 市区町村の調べ直しをとばす
python3 livecam-db/build.py --only jice-roads   # 情報源をしぼる
```

**⚙️ 自動更新**：`.github/workflows/livecam-update.yml` が**毎週火曜 5:10（日本時間）**に
上の2つを実行します。カメラは増えたり減ったり故障したりするので、週1で見直す形にしています。

---

## 🌍 世界のライブカメラ台帳（試験）

2026-08-16 から、**世界のカメラ用の別台帳**を試験的に始めました。

```bash
python3 livecam-db/build_world.py            # 🌍 世界台帳を作りなおす
python3 livecam-db/build_world.py --no-verify  # 写真の確認をとばす
```

- できあがる場所：`data/livecams_world.json`（日本の `livecams.json` とは**別ファイル**）
- **日本のアプリには配りません**（`export.py` の `traffic-app` 向けは日本の台帳だけを配るので、交通ページは重くなりません）。
- 📺 **配り先：`world-livecam/`（🌍 世界のライブカメラのページ）**。2026-08-16 に新設しました。
  `python3 livecam-db/export.py --target world-livecam` で、ページ用のデータと
  ページ内の埋め込みを同時に書き換えます（ページ側を直接いじらないこと）。
- 市区町村付け（国土地理院＝日本専用）は使えないため、場所は「アメリカ・アイオワ州」のような州単位です。
- 🔄 **更新は週1**（毎週火5:10の `livecam-update.yml` に相乗り）。
  世界分の取得が失敗しても日本の更新は止めません（`continue-on-error`）。
  失敗時は `build_world.py` が既存ファイルを書き換えずに終わるので、前回の台帳が残ります。

| 情報源 | 中身 | 利用条件 |
|---|---|---|
| 🇺🇸 アイオワ州交通局（Iowa DOT） | 高速道路（I-系）・一般道・休憩施設・道路気象カメラ 約1,200件 | **CC BY 4.0**＝出典を書けば再利用可。出典：Iowa Department of Transportation - Office of Traffic Operations |

> 🔎 **なぜアイオワ州から？** 2026-08-16 に米国5州を調査した結果、「機械で読める一括データ」と
> 「はっきりした利用許可」の両方がそろっていたのがアイオワ州だけだったためです（下の記録を参照）。

### 📋 世界の情報源の調査記録（2026-08-16 時点・同じ調査を繰り返さないため）

| 情報源 | 判定 | 理由 |
|---|---|---|
| 🇺🇸 アイオワ州 DOT | ✅ 取り込み済み | CC BY 4.0 明記・画像は最初からhttps・鍵なしGeoJSONで一括取得可 |
| 🇨🇦 カナダ ブリティッシュコロンビア州 DriveBC | ✅ **取り込み済み**（2026-08-19・1,026台） | Open Government Licence – British Columbia 明記・画像は最初からhttps・鍵なしJSONで一括取得可。**他所が写真を提供している34台は除外**（→ 下の「🇨🇦 BC州の取り込みの記録」） |
| 🇦🇺 オーストラリア NSW州 Transport for NSW | ⏸ 保留 | ライセンスは CC BY で問題なし。ただしデータ取得に**APIキーの登録が必要**＝のんラボの「鍵ゼロ」方針に合わない（Windyと同じ理由） |
| 🇨🇦 カナダ オンタリオ州 511 Ontario | ⏸ 保留 | 公的ライセンス（Open Government Licence）＋鍵なしで一覧は取れるが、**写真URLが確かめられなかった**（ページをJavaScriptで組み立てる作りのため）。深掘りすれば候補になりうる |
| 🇺🇸 オレゴン州 ODOT・ペンシルベニア州 511PA | ⏸ 保留 | 「使ってよい・出典を書いてね」の案内はあるが、CC BY のような**はっきりしたライセンス文が無い**。データ取得にも登録が必要 |
| 🇺🇸 ジョージア州 GDOT | ❌ 見送り | ライセンス欄が空（許可の明記なし）＋画像サーバーが外部からの接続を遮断している兆候（栃木県と同じパターン） |
| 🇺🇸 ワシントン州 DOT | ⏸ 保留 | 公式データはあるが、ライセンス欄が免責事項のみで「再配布してよい」の明言なし。問い合わせれば化ける可能性あり<br>（2026-08-16 再確認：**変化なし**。「低頻度の利用のみを想定」という制限が書かれているだけだった） |
| 🇺🇸 フロリダ州 | ⏸ 保留 | 見つけたデータセットの持ち主が個人アカウントらしく、公式の公開データと言い切れない<br>（2026-08-16 再確認：**変化なし**。州の公式オープンデータの一覧にカメラのデータ自体が無い） |
| 🇺🇸 ミネソタ州 DOT | ⏸ 保留 | 一括データの入口が見つからず<br>（2026-08-16 再確認：**変化なし**。州公式の一括データは見つからず、郡が独自に写した地図データがあるのみ） |
| 🌐 Windy Webcams API（民間・世界最大級） | 🚫 条件に合わず | APIキー登録が前提（のんラボの「鍵ゼロ」方針に合わない）。無料枠は低解像度・画像リンク10分失効。リンクで案内 |
| 🇰🇷 韓国 ITS・🇹🇼 台湾 高速公路局ほか各国の公式サイト | 🔗 リンクで案内 | `traffic-app` の「🌍 世界のライブカメラ」欄から公式ページへ案内（転載なし＝権利問題なし） |
| Insecam系（無断で監視カメラを集めたサイト） | 🚫 絶対に使わない | 所有者に無断＝プライバシー侵害のため、調査対象にもしない |

### 🇨🇦 BC州の取り込みの記録（2026-08-19）

定期調査（2026-08）で有望候補になっていたBC州を、実際に台帳へ入れました。**世界台帳の2つ目の情報源**です。

| 確かめたこと | 結果 |
|---|---|
| ライセンス | BC州の公式データカタログのAPIで `Open Government Licence - British Columbia` を確認 |
| データ | `https://www.drivebc.ca/api/webcams/` から鍵なしで **1,060台**（緯度経度つき） |
| 写真 | `https://www.drivebc.ca/images/<番号>.jpg` が本物のJPEGを返すことを3台で実測（HTTP 200・`image/jpeg`） |

> 📄 **出典の文言（消さないこと）**：
> `Contains information licensed under the Open Government Licence – British Columbia.`
> → `world-livecam/index.html` のページ下部と、台帳の `sources` に入れてあります。

#### ⚠️ いちばん大事な判断：`credit` 欄の仕分け

前回の調査記録には「**26台ほどは他所が提供しているカメラ**」と書いてありましたが、
実際に全件を調べたところ **82台に `credit` が入っていて、その多くは提供元ではなく運用メモ**でした。

| 中身 | 台数 | どうしたか |
|---|---|---|
| 他所が写真を提供している（TransLink・Parks Canada・City of Vancouver など） | **34台** | ❌ **取り込まない**（州のライセンスの外にある可能性があるため） |
| ただの運用メモ（「太陽光発電なので遅れます」「監視のため向きが変わります」など） | **48台** | ✅ 取り込む（州が自分で運用しているカメラ） |

**82台を丸ごと除くと、州のカメラ48台を誤って捨てることになります。**
逆に1台も除かないと、ライセンスの外の写真を載せてしまいます。
そこで「**他所が写真を提供していると読める言い回し**」だけを除く方式にしました
（`provided by` / `courtesy of` / `co-operation with` / `installed and maintained by`）。
安全側に少し広めで、電源提供の2台も除いています。

#### そのほかの判断

- **高速道路の区別はしていません**（全部 `road`）。アイオワ州は路線名が `I-80` のように高速と分かりますが、
  BC州のデータには高速かどうかを見分けられる印がありません。**分からないものを決めつけない**ためです。
- **止まっているカメラ15台は写真なし扱い**にしました（古い写真を「今の様子」として見せないため）。カメラ自体は残し、公式ページへの案内にしています。
- 場所は、データに入っている地域名を日本語にして「カナダ・BC州（ローワーメインランド）」の形にしました。

#### 📄 あわせて直したもの

- `livecam-db/export.py`：圧縮データに **`s`（情報源）の欄を追加**。これが無いと、世界のページの
  「🇺🇸アイオワ州だけ／🇨🇦BC州だけ」のしぼりこみが**全部0台**になります（実際に一度そうなり、実ブラウザで発見して修正）。
- `world-livecam/index.html`：地域のしぼりこみボタン・出典欄（2つの情報源）・タイトル・説明文を更新。
  検索は名前だけでなく**場所でも**引けるようにしました（例：「バンクーバー島」で182台）。

### 🗓 定期調査の記録（毎月20日・自動実行「🌍 世界カメラ情報源の定期調査」）

#### 2026-08-16（1回目）

**🆕 いちばんの収穫：カナダ ブリティッシュコロンビア州 DriveBC**

のんラボが情報源に求める3つの条件を、**実際にデータを取って**確かめました。

| 条件 | 確かめた結果 |
|---|---|
| ① はっきりした利用許可 | **Open Government Licence – British Columbia**（州の公式カタログで確認）。複製・改変・配布・商用利用まで認められ、条件は**出典を書くことだけ** |
| ② 機械で読める一括データ | `https://www.drivebc.ca/api/webcams/` から**鍵なし**でJSONが取れる（**1,060台**・緯度経度つき） |
| ③ https の写真 | `https://www.drivebc.ca/images/<番号>.jpg` が実際に画像として返ってきた（1,060台すべてに「httpsで見られる」の印つき） |

> 📄 **取り込むときに書く出典**（ライセンス本文が指定している文言）：
> `Contains information licensed under the Open Government Licence – British Columbia.`

> ⚠️ **取り込むときの注意**：1,060台のうち**26台ほどは他所が提供しているカメラ**です
> （TransLink 8台・Parks Canada とアルバータ自動車協会 7台・AtkinsRéalis 4台など。データの `credit` 欄に書かれています）。
> 州のライセンスの外にある可能性があるので、**足すときは `credit` 欄が空のものだけにする**のが安全です。

**既存の取り込み先（3つとも変わりなし）**

| 情報源 | 確認結果 |
|---|---|
| 🇺🇸 アイオワ州 DOT | **CC BY 4.0 のまま**（州の利用条件ページで確認）。データも今まで通り取れました（**1,207台**） |
| 🚢 海上保安庁 | **PDL1.0 のまま**（出典を書けば再利用可） |
| 🏞 JICE（道路・河川チェック地図） | 変化なし（国土地理院の利用規約に準拠、という記載のまま） |

**次回への申し送り**

- 🇯🇵 富山県：道路情報サイト自体には今回も利用条件の記載が見つからず、**保留のまま**。
  県の公式サイト全体の著作権表記に手がかりがありそうでしたが、当日はサーバーが応答せず**確かめられませんでした**。次回はそこを見ること。
- 🇨🇦 オンタリオ州：写真URLの取り出し方がわかれば候補になります。
- 🇯🇵 東京都・大阪府・愛知県・北海道のオープンデータは、機械で読めるカメラの一括データが見当たらず（調査済み）。

### 安全装置

- 取得に失敗したら、**今ある台帳は上書きしません**
- カメラが極端に少ない結果になったときも上書きしません（こわれたデータで壊さないため）
- **情報源ごと**に前回の70%を下回ったら中止します
  （合計だけ見ていると「道路が全部消えても河川が残っているから合格」になってしまうため）
- 通信の失敗は**カメラ1台ぶんの失敗として扱い**、全体を止めません
  （1台の不調で週1の更新が丸ごと止まらないように）
- 同じ座標に3種類以上の名前が登録されていたら**警告を出します**
  （元データのまちがいに気づくため。実際に北海道の8河川が同じ1点に登録されている例があります）
- 案内先（公式ページ）の**ドメインが消えていたら警告を出します**
  （映像が出せないカメラは公式ページへ案内するので、その先まで消えていると行き止まりになるため。
  2026-08-15 時点で **116台ぶん**が該当。確かめるのは名前が引けるか＝DNSだけなので軽く、
  相手のサーバーにも負担をかけません。**消しはせず、知らせるだけ**です）

---

## 新しい情報源の足し方

1. `sources/` に1ファイル追加して、`collect_all()` を用意する
   （返り値は「カメラ一覧」と「情報源の記録」の2つ）
2. カメラ1台の形は上のスキーマにそろえる（`img` は http のままでOK。確認は `build.py` がやります）
3. `build.py` の `COLLECTORS` に登録する

### 📋 調べたけれど取り込まなかった情報源（2026-08-15 時点）

「もっとカメラを増やせないか」を調べた結果の記録です。**同じ調査を繰り返さないため**に残しています。

#### ❌ 取り込まない：再利用の禁止が明記されているもの

| 情報源 | 件数 | 明記されている内容 |
|---|---|---|
| 福井県 みち情報ネットふくい | 約220 | 「当ホームページに掲載されている内容の**無断転載を禁じます**」 |
| 群馬県 ぐんまの道路ライブカメラ | 130 | 「著作権法上認められた場合を除き**無断転載を禁じます**」 |
| 青森県 青森みち情報 | 多数 | 「**無断で転用・引用することはできません**」 |
| 民間のライブカメラまとめサイト | 3万件超 | 明記は無いが、**人手で整理された名簿そのものが商品**。日本の著作権法では、選び方や並べ方に工夫のあるデータベースにも権利が生じえます |
| **NEXCO東日本（ドラぷら）** | 多数 | 利用規約に「**他のサイトや印刷媒体に転載したり、商用目的として利用することはできません**」と明記。NEXCO中日本・西日本、首都高、阪神高速なども同様の民間企業サイト |

→ **リンクで案内する**方針にしました（`traffic-app` の「各県の道路カメラサイト」欄）。
   どうしても台帳に入れたい場合は、**各担当課に一報して許可を得てから**足してください。

#### ⏸ 保留：明記が無いもの

| 情報源 | 件数 | 状況 |
|---|---|---|
| 富山県 道路情報 | 174 | JSON・緯度経度・https画像がそろっていて技術的には理想的。ただし利用条件の記載が見つからず、**「書いていない＝使ってよい」ではない**ため保留<br>（2026-08-16 再確認：サイト内の説明ページを見直しても利用条件の記載はやはり無く、**保留のまま**） |

#### 🚫 条件に合わなかったもの

| 情報源 | 理由 |
|---|---|
| 国土交通省 水防災オープンデータ提供サービス（CCTV約3,000＋簡易カメラ約4,000） | **有償**（実費相当額）。※ JICE の2つの地図データは、これを国が無料で配っている経路とみられます |
| 国土交通省 道路情報提供システム（全国の地方整備局共通） | 全国規模になりうるが、**防御装置に阻まれて到達できず**（日本国内からなら取れる可能性あり） |
| 栃木県 河川ライブカメラ（CC BY 4.0） | ライセンスは問題なし。ただし画像の配信元が**海外からのアクセスを遮断**しており、GitHub Actions から取得できない見込み |
| 島根県・石川県・岐阜県・長野県・秋田県・福島県・鳥取県・新潟県 | 機械で読める一括データが見つからず（HTMLのみ／SPAの内部APIを特定できず） |
| 京丹後市（CC BY 4.0・8件）／北九州市（CC BY・6件） | ライセンスは問題なし。**規模が小さい**ため今回は見送り。増やしたくなったら足せます |

### 🚫 写真として使わないホスト

元データ（JICE）の「写真URL」欄に、まれに**第三者のまとめサイトに置かれた画像**が混ざっています。
実際に `livecam.asia` の **2016〜2017年のスクリーンショット**が **43台ぶん**入っていました。

そのまま出すと、

1. **何年も前の静止画を「今の様子」として見せてしまう**（いちばん困る）
2. よそのサイトが置いているファイルを直接読みに行くことになる

ため、`sources/base.py` の `BLOCKED_IMAGE_HOSTS` に入れて**写真としては使いません**
（カメラ自体は残し、公式ページへのリンクで案内します）。

あわせて、`go.jp` / `lg.jp` / 生のIPアドレス以外のホストは**気づけるよう知らせます**
（止めはしません。鳥取県の雪みちNAVI のように、公的な情報を別ドメインで出している例もあるため）。

### 情報源を選ぶときの約束 🚧

- **公的機関または公的性格の団体が公開しているもの**を使う
  （国土交通省・各地方整備局・自治体・独立行政法人など）
- **民間のまとめサイトの一覧を、まるごと写して使わない**。
  他人が時間をかけて集めた名簿そのものには権利が生じうるため
  （日本の著作権法でも、選び方や並べ方に工夫のあるデータベースは保護されえます）。
  カメラの映像そのものは各管理者のものを直接見に行くのが筋です。
- 相手のサーバーに負担をかけない（同時アクセス数は控えめに、更新は週1程度に）

---

## ファイルの中身

| ファイル | 役割 |
|---|---|
| `build.py` | 台帳を作る司令塔（集める→まとめる→確認→保存） |
| `build_world.py` | 🌍 世界台帳（試験）を作る司令塔（日本版とは別ファイルを作る） |
| `export.py` | 台帳を各アプリに配る係（`traffic-app`＝日本の台帳／`world-livecam`＝世界の台帳） |
| `livecam.py` | 台帳を**使う**ための小さな道具（近くを探す・ルート沿いを探す・検索） |
| `sources/base.py` | どの情報源でも使う共通の道具（取得・https確認・市区町村） |
| `sources/jice.py` | 情報源①：JICE の道路／河川ライブカメラ |
| `sources/kaiho.py` | 情報源②：海上保安庁のライブカメラ（灯台・港・岬） |
| `sources/us_iowa.py` | 情報源③：🌍 アイオワ州交通局の道路カメラ（世界台帳・試験） |
| `data/livecams.json` | できあがった台帳（**大もと**） |
| `data/livecams_world.json` | 🌍 世界台帳（試験・**日本のアプリには配らない**） |

すべて **Python 3 の標準機能のみ**で動きます（追加インストール不要）。
